# 2026-09-02 开发日志

## 今日目标

- 完成 S16-05：按用户要求重做并充实国创赛项目计划书，形成内容完整、版式统一、原生可编辑且通过逐页检查的最终 Word。
- 只读盘点当前系统的未完成项，并复核现有 iOS 与 AI 代理测试基线。
- 开始 S10-12：补齐微计划版本更新、跨天、提前结束和删除后的 CareKit/SwiftData 历史一致性。
- 开始 S07-02：收口通用最近 7 个日历日窗口，统一有效日、缺失值和时区边界语义。
- 开始 S07-03：基于 7 天当前窗口和 28 天个人基线，实现可追溯的变化幅度和双窗口覆盖率。
- 开始 S07-04：使用集中配置的稳健离散门槛识别孤立单日异常，并接入“今日变化”的输出守门。
- 开始 S07-05：组合数据质量、受保护变化幅度和同向日证据，输出可追溯的三档趋势结果。
- 开始 S07-06：集中并版本化睡眠、HRV、静息心率和步数四类核心指标阈值，同时保持今日页既有扩展候选兼容。
- 开始 S07-07：用四类核心指标的端到端合成 Fixture 覆盖稳定、持续升降、数据不足与中间断档。
- 开始 S07-08：在指标详情页展示趋势计算依据、双窗口有效样本天数、时间范围和数据质量降级原因，同时保持“今日变化”卡既有两段式内容不变。
- 开始 S08-01：建立每日主观记录与生活事件的领域模型、独立 SwiftData Schema V1 和显式迁移计划，不提前实现签到界面。
- 开始 S08-02：在今日页接入精力、压力和身体感受三项快速签到，复用 S08-01 本地存储，并守住历史日期、演示模式和保存失败边界。

## 已完成事项

### 06:57｜S16-05｜项目计划书 V3.0 最终交付

- 完成内容：将此前版本重做为 26 页黑白 A4 原生可编辑 Word；保留封面必要留白，其余页面采用正文、三线表、证据卡片、图表和产品截图充实排版。全文包含 290 个原生段落、45 组真正三线表、12 幅内嵌图与产品截图，正文及表格约 14376 个汉字。
- 内容范围：补全项目摘要、政策与行业背景、用户问题、需求与调研设计、产品定位、功能闭环、产品展示、技术架构、算法与数据质量、AI 安全、竞品分析、商业模式、推广策略、团队分工、进度计划、财务测算框架、风险控制、社会价值、比赛评分对照、参考来源和三项提交附录。
- 真实性处理：公开用户与竞品数据均保留可核验来源；尚未完成的一手问卷、访谈、收入、用户量、合作、知识产权和医疗效果明确写为拟开展、预测或待核验，没有虚构为既有成果。
- 修改文件：`竞赛材料/build_zhiheng_competition_book_editable.py`、`竞赛材料/fontconfig-codex.xml`、`output/word/知衡——从健康洞察到行为改变的AI个人健康智能体_国创赛项目计划书.docx`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：使用文档渲染流程生成 26 张页面图并逐页检查，未发现空白尾页、正文溢出或截图裁切；45 组表格的表宽、缩进、网格与单元格宽度全部一致；12 幅图片均为内嵌图片；标题审计识别 37 个一级标题；无障碍审计高、中、低风险均为 0。最终 Word 大小约 1.55 MB。
- 关键决定：坚持黑白竞赛材料风格和真正三线表；图表与产品界面使用图片，正文与表格保持原生可编辑；不擅自补写指导教师、专业年级、真实调研结果、收入融资和知识产权等未提供事实。

### 16:31｜系统未完成项盘点｜只读复核

- 检查范围：对照 `docs/development-requirements.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、当日日志与当前 Swift/AI 代理源码，核对未完成任务和代码边界。
- 盘点结论：当前唯一紧接开发任务仍为 S10-12；现有计划服务尚无正式的计划修改和删除接口，删除后 CareKit 历史、Outcome 与 SwiftData 基线的一致性尚未验收。后续核心范围仍包括 S07 通用趋势收口、S08 主观签到与生活情境、S09 可解释洞察、S11 “我的有效方法”以及 S13～S15 的通知、发布安全和真实用户验证。
- 代码佐证：当前工程中未定义 `DailyCheckIn`、`ContextEvent`、`EffectiveMethod`、`NotificationService`或通用 `TrendEngine`；`CarePlanService` 也没有计划修改/删除方法。
- 验证结果：在 iPhone 17 Pro（iOS 26.5）模拟器执行完整 iOS 测试，162 项全部通过、0 失败、0 跳过；执行 AI 开发代理测试，14 项全部通过。本次未修改代码、任务状态或产品范围。
- 剩余风险：自动化测试通过只证明已实现部分未回归，不代表未实现的主观记录、生活情境、有效方法、通知、用户全量删除和真实用户实验已经通过。

### 16:50｜S10-12｜计划更新、跨天、删除与历史一致性

- 完成内容：保留既有 CareKit 版本化暂停、恢复和提前结束路径，新增历史页单计划删除入口与不可恢复二次确认；删除同步覆盖该计划的 CareKit Outcomes、任务、计划和按 `carePlanID` 关联的 SwiftData 评估基线，成功后即时刷新当前计划与历史，不触碰其他计划。
- 修改文件：`CarePlanService.swift`、`CareKitPlanStore.swift`、`MicroPlanView.swift`、`CareKitPlanStoreTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/development-requirements.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增跨两个日历日分别保存完成/跳过、删除已结束计划后完整清理、删除一个计划不影响另一历史计划、磁盘重建后删除状态不复活 4 项测试。CareKit 计划专项 20 项全部通过；完整 iOS 测试 166 项全部通过、0 失败、0 跳过；AI 代理测试 14 项全部通过；Debug 通用模拟器构建和 Release 通用真机构建均成功。
- 发现与修正：首次新增测试因在 XCTest 自动闭包中直接 `await` 而编译失败，拆出异步结果后修正；首次删除测试又发现 CareKit 删除 `OCKCarePlan` 只建立版本墓碑，不会自动隐藏独立版本化的任务和 Outcome，遂改为显式删除 Outcomes、Task 和 CarePlan，并让已删除任务的结果查询稳定返回不存在。失败均已如实保留在本日志，最终回归通过。
- 关键决定：单计划删除只开放在已结束历史页，避免把“终止计划”和“删除历史”混成一个操作；删除文案明确不可恢复，且只有两类存储都完成后才显示成功。S10-12 完成后回到起点审计顺序，下一项只推进 S07-02 通用最近 7 天窗口计算收口。

### 17:19｜S07-02｜通用最近 7 天窗口计算收口

- 完成内容：新增纯 Swift 的 `HealthMetricRecentWindowEngine`，统一以参考日为末日的 7 个日历日窗口；在至少 4 个有效日时输出指标、半开时间范围、预期/最低/实际有效日、真实每日点位、单位和中位数，少于 4 日时返回可追溯的数据不足结果；缺失日始终不补 0。
- 接入与确定性：“今日变化”已移除重复的 7 天取点、有效日门槛与中位数计算，改为复用新引擎；当同等优先级来源在最新时间和样本数上完全平局时，以稳定来源标识打破平局，保证输入重排后结果不变。
- 修改文件：`HealthBaseline.swift`、`HealthDataQuality.swift`、`TodayDashboardPresentation.swift`、`HealthBaselineTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增断续 4 天、仅 3 天、窗口前后边界、跨夏令时和来源输入重排 5 项测试；窗口与今日变化专项 25 项通过。完整 171 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Debug 测试构建与 Release 通用真机无签名构建均成功。
- 验证过程：首次在沙箱内调用 Xcode 因无法写入模拟器与 SwiftPM 缓存而失败，获准在沙箱外按同一命令重跑后通过；完整测试与 Release 构建的首次权限自动审核均曾超时，同命令各重试一次后成功，属于环境权限问题，不是代码失败。
- 关键决定：S07-02 只收口当前窗口与中位输入，不提前实现 S07-03 的变化幅度和覆盖率，也不改写 S06-30 已验收的稳健变化与低焦虑表达规则。

### 18:02｜S07-03｜变化幅度和双窗口覆盖率

- 完成内容：新增纯 Swift 的 `HealthMetricChangeMagnitudeCalculator`，只接受指标、单位一致且前后有序、不重叠的可用基线与当前窗口；统一输出两个窗口及其中位值、绝对变化、可选相对变化、有效日、预期日和覆盖率。基线中位值为 0 时保留绝对变化并明确不给百分比，避免产生无穷大或伪造结果。
- 接入与边界：“今日变化”改为复用统一变化结果，不再自行重复计算相对变化；本轮只收口变化幅度与覆盖率，没有提前实现 S07-04 单日异常值保护或 S07-05 三档趋势判断。
- 修改文件：`HealthBaseline.swift`、`TodayDashboardPresentation.swift`、`HealthBaselineTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增下降与双窗口覆盖率、上升方向、无变化、零基线、指标/单位/窗口非法比较 5 项测试；变化与今日展示专项 30 项全部通过。完整 176 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Release 通用真机无签名构建成功。
- 验证过程：首次专项构建因新增测试夹具中两个函数调用误用右花括号而编译失败；修正时一次过宽替换又影响了前文两个花括号，经逐行检查立即恢复。第二次构建因 `XCTUnwrap` 闭包返回类型无法推断而失败，补充明确的可选类型后第三次重跑通过。以上均为测试代码编译问题，最终完整回归与 Release 构建通过。
- 关键决定：覆盖率分别保留基线 28 天与当前 7 天的分子、分母和比例；相对变化只在非零基线时存在。非法比较直接返回领域错误，不让 UI 或 AI 接收到看似有效但语义不一致的百分比。

### 18:23｜S07-04｜单日异常值保护

- 完成内容：新增纯 Swift 的 `HealthMetricSingleDayOutlierGuard`，以当前中位数为中心，使用当前窗口 MAD 与个人基线 MAD 中较大者乘以 3.5 的门槛识别孤立日期；门槛与版本 `s07-outlier-v1` 集中保存在 `HealthDataQualityThresholds`，结果保留指标、单位、窗口、样本数、两类 MAD、门槛、越界日数和孤立点。
- 保护语义：只有恰好一个日期越界时才在分析副本中排除该点，原始窗口、覆盖率和图表数据保持不变；随后通过 S07-03 的统一计算器重算受保护的中位值、绝对变化与相对变化。排除后不足 4 个分析日时不输出；若其余日期仍持续变化，则继续进入既有幅度与一致性门槛，不让一个异常日反向遮蔽真实多日变化。
- 接入范围：“今日变化”已消费受保护结果；本轮没有提前输出 S07-05 的“未见明确变化／值得继续观察／存在持续变化”三档结论。
- 修改文件：`HealthDataQuality.swift`、`HealthBaseline.swift`、`TodayDashboardPresentation.swift`、`HealthBaselineTests.swift`、`TodayDashboardPresentationTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增孤立异常、持续移动、多日离散、输入重排和持续变化伴随一个异常日的页面接入 5 项测试；基线与今日页专项 35 项全部通过。完整 181 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Release 通用真机无签名构建成功。
- 验证过程：首次在沙箱内执行专项测试因模拟器服务和 SwiftPM/Clang 缓存无写权限而失败，获准在沙箱外按同一命令重跑后通过，属于环境权限问题。首版接入采用发现异常即停止输出的保守方式；代码审查发现这会遮蔽“多数日期持续变化、另有一个异常日”的真实趋势，遂在完整回归前调整为保留原始数据、仅在分析副本中排除并重算，新增对应回归测试后通过。
- 关键决定：异常值保护只回答“是否存在一个孤立日期及排除后的稳健变化”，不把数值称为健康异常，也不负责三档趋势分类；多日离散不擅自归因成一个异常日。

### 18:47｜S07-05｜三档趋势结果

- 完成内容：新增纯 Swift `HealthMetricTrendDetector`，把可用基线、当前窗口、来源稳定状态和调用方最小相对阈值组合成统一趋势事实；只输出“未见明确变化／值得继续观察／存在持续变化”三档，并保留算术方向、受保护变化、双窗口范围与覆盖率、分析点、异常点、固定/稳健门槛、同向日和两套阈值版本。
- 判定规则：先执行 S07-04 异常保护；来源不稳定、异常排除后少于 4 个分析日或零基线无法计算相对变化时统一降级为“值得继续观察”。其余结果使用调用方最小相对阈值与 `2.5×基线 MAD÷|基线中位值|` 中较大者：未达门槛为“未见明确变化”，达到门槛但同向日不足为“值得继续观察”，达到门槛且同向日不少于 3 天并不少于分析日一半才是“存在持续变化”。
- 接入范围：“今日变化”已移除重复的幅度门槛与同向天数判断，只消费通用判定器的“存在持续变化”结果并继续按变化强度排序；各指标专属阈值表仍留给 S07-06。
- 修改文件：`HealthDataQuality.swift`、`HealthBaseline.swift`、`TodayDashboardPresentation.swift`、`HealthBaselineTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增低于门槛、同向日不足、持续变化、来源/保护后样本受限、零基线、稳健 MAD 门槛和非法配置 7 项测试；基线与今日页专项 42 项全部通过。完整 188 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Release 通用真机无签名构建成功。
- 关键决定：`higher/lower` 只保留数值相对基线的算术方向，不解释为健康变好或变差；三档结论由本地确定性程序生成，AI 与页面不得自行改写。

### 19:08｜S07-06｜四类核心指标集中阈值配置

- 完成内容：新增版本化 `HealthMetricTrendThresholdCatalog`，把睡眠时长 8%、HRV 15%、静息心率 8% 和步数 15% 四类核心最小相对变化门槛集中到领域层，版本统一为 `s07-metric-thresholds-v1`；目录同时明确返回指标、数值和可直接传给趋势判定器的配置。
- 兼容处理：S06-30 已验收的“今日变化”实际支持六类候选，因此同一目录保留活动能量 18% 与锻炼时长 25% 两个扩展门槛，但通过 `coreMetrics` 与 `todayCandidateMetrics` 明确区分；未配置指标返回 `nil`，不采用猜测的通用默认值。
- 接入范围：“今日变化”配置只保留指标和排序优先级，六个百分比已全部移出页面展示模型；趋势结果保存本次实际使用的目录版本。本轮沿用既有产品默认数值，没有把它们描述为医学界限，也没有在缺少真实数据证据时调参。
- 修改文件：`HealthBaseline.swift`、`TodayDashboardPresentation.swift`、`HealthBaselineTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增四类核心集合/数值/版本、两个扩展兼容与未配置指标拒绝、目录版本传入趋势结果 3 项测试；基线与今日页专项 45 项全部通过。完整 191 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Release 通用真机无签名构建成功。
- 关键决定：阈值目录是指标最小相对变化的唯一事实来源；后续调整必须更换版本、添加或修改合成 Fixture，并在日志说明真实问题和调整影响。

### 19:22｜S07-07｜四类核心指标趋势场景测试

- 完成内容：新增 5 项端到端合成 Fixture 测试，让睡眠时长、HRV、静息心率和步数四类核心指标逐一经过 28 天基线、7 天当前窗口、变化幅度、单日异常保护和三档趋势判定；实际覆盖稳定、持续上升、持续下降、当前窗口不足、基线不足和中间断档 20 组指标场景。
- 数据边界：当前窗口不足固定验证 3/7 有效日，基线不足固定验证 13/28 有效日；中间断档固定验证 4/7 非连续有效日仍可形成趋势，所有缺失日期均保持缺失且不补 0。合成升降只验证算术方向和确定性分类，不解释为健康变好或变差。
- 修改文件：`HealthBaselineTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：`HealthBaselineTests` 34 项全部通过；完整 196 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Release 通用真机无签名构建成功。
- 关键决定：本轮只扩展测试证据，不修改生产代码、趋势算法或已版本化阈值；后续真实数据校准仍需单独版本、证据和影响记录。

### 20:38｜S07-08｜指标详情趋势依据与样本天数

- 完成内容：新增纯 Swift `HealthMetricTrendEvidenceBuilder`，组合既有 7 天当前窗口、此前不重叠的 28 天个人基线、来源稳定判断和三档趋势结果；支持趋势阈值的指标详情在趋势图下方新增“趋势依据”卡，展示双窗口日期、有效样本天数、中位数对比、相对变化、实际门槛、同向日与判定状态。
- 数据质量边界：当前窗口不足、基线不足、来源变化与三档趋势使用互斥状态；孤立日期被保护时明确说明原始记录仍留在图表。未配置指标不显示证据卡、不猜测阈值；“今日变化”卡继续只显示“发生了什么”和“建议”。来源稳定判断从今日页私有实现提取为共用领域能力，避免两处口径漂移。
- 修改文件：`HealthBaseline.swift`、`TodayDashboardPresentation.swift`、`HealthMetricDetailView.swift`、`HealthBaselineTests.swift`、`AGENTS.md`、`docs/README.md`、`docs/development-requirements.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增可用证据、当前窗口不足、基线不足、来源变化和未配置指标拒绝 5 项测试；基线与今日页专项 55 项全部通过。完整 201 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Release 通用真机无签名构建成功。使用 iPhone 17 Pro（iOS 26.5）模拟器通过既有 Debug 定位参数进入静息心率详情，确认趋势图下方出现“趋势依据”和“未见明确变化”状态，卡片未挤压趋势图或底部导航。
- 验证过程：首次专项构建发现一处可选百分比未解包，以及补丁转义导致 Swift 字符串插值符丢失；修正后专项与完整回归均通过。模拟器服务中途重启一次，重新启动设备后使用同一构建完成实际导航，不属于代码失败。
- 关键决定：完整技术证据放在指标详情，不破坏今日变化卡已冻结的简洁两段式内容；相对升降只描述算术方向，不表示健康变好或变差。

### 21:06｜S08-01｜主观记录 SwiftData 模型与迁移策略

- 完成内容：新增 `DailyCheckIn`、`ContextEvent` 领域模型及对应 SwiftData 实体和存储适配层；签到保存精力、压力、身体感受 1～5、本地公历年月日、记录时区、可选短备注与时间戳，同一日再次保存更新原记录；生活事件独立保存稳定类型、自定义标签、可选起止时间、强度、短备注与时间戳。
- 迁移与职责：建立独立 `ZhihengSubjectiveRecords` 本地配置、`SubjectiveRecordsSchemaV1` 和显式 `SubjectiveRecordsMigrationPlan`。V1 是首个结构，当前无旧版本迁移阶段；未来字段变更必须新增 Schema 版本和相邻迁移阶段，不得原地改写 V1。该存储不保存 HealthKit 原始样本，也不重复 CareKit 计划完成状态；自由文本未写入技术日志。
- 修改文件：`SubjectiveRecordModels.swift`、`SubjectiveRecordStore.swift`、`SubjectiveRecordStoreTests.swift`、`Zhiheng.xcodeproj/project.pbxproj`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增 6 项测试，覆盖 Schema/迁移计划挂接、磁盘 Store 释放后重建读回、同一日期只保留一条签到、生活事件时间重叠筛选、签到与事件独立删除，以及非法时间/缺少自定义标签/超长文本拒绝。专项 6 项通过；完整 207 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理 14 项通过；Release 通用真机无签名构建成功；工程文件格式和 `git diff --check` 通过。
- 关键决定：S08-01 只建立可靠的数据和迁移底座，不提前实现签到 UI。日期身份使用记录发生时的本地公历年月日，并同时保存时区，避免仅按 UTC 日期导致跨时区错日；主观记录与生活事件使用独立实体，便于后续分别编辑、删除和按时间线对齐。

### 21:38｜S08-02｜今日页三项快速签到

- 完成内容：在今日页新增“今天感觉怎么样？”签到卡，提供精力、压力和身体感受三项 1～5 快速选择；三项全部选择后才允许保存，同一天再次保存更新原记录。成功后显示本机保存时间，重启应用自动恢复当天选择。签到卡只在当天出现，历史日期不允许补填。
- 失败与隐私边界：通过共享 `SubjectiveCheckInSession` 连接 S08-01 的本地存储；保存失败时保留用户刚选的草稿并显示可重试提示，不显示假成功。演示数据模式明确禁用主观记录读写，避免模拟选择混入真实记录；不可用存储统一返回失败状态，不导致页面崩溃。
- 修改文件：`SubjectiveCheckInSession.swift`、`SubjectiveRecordStore.swift`、`TodayView.swift`、`RootTabView.swift`、`SubjectiveRecordStoreTests.swift`、`Zhiheng.xcodeproj/project.pbxproj`、`AGENTS.md`、`docs/README.md`、`docs/technical-design-spec.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-02.md`。
- 验证结果：新增恢复既有记录、三项完整性、正确本地日期保存、失败保留草稿和演示模式隔离 5 项会话测试；主观记录专项 11 项全部通过。完整 212 项 iOS 测试全部通过、0 失败、0 跳过；AI 代理测试 14 项通过；Release 通用真机无签名构建成功；工程文件格式和 `git diff --check` 通过。
- 实际交互：在 iPhone 17 Pro（iOS 26.5）模拟器逐项选择“精力较好、压力一般、身体轻松”后保存，按钮切换为“更新今天状态”并显示本机保存时间；终止并重启应用后，三项选择和保存时间均恢复。可访问性树可识别 15 个选项及保存按钮，视觉检查未见文字重叠或卡片挤压。此处只验证交互闭环，正式的约 10 秒计时留给 S08-03。
- 关键决定：S08-02 不提前加入生活事件、备注、历史编辑或时间线；主观感受必须由用户主动选择，不根据 HealthKit 指标自动填充，也不用客观数据否定用户感受。

## 待办事项

- [ ] 获得学校最终模板、赛道名称和指导教师信息后，按报名系统逐项替换封面及团队信息。
- [ ] 团队按附录方案开展真实问卷和半结构访谈，并用可追溯证据替换文中的“拟开展”表述。
- [ ] 提交前重新核对工程最新测试数量、产品截图日期、比赛规则和所有外部链接。

## 问题与风险

- 当前公开研究能够支持行业问题与竞品判断，但不能替代团队自己的一手用户证据；若不补充真实调研，答辩时需求真实性的说服力仍有限。
- 财务、合作、用户规模、知识产权和医学有效性均没有可核验既有成果，正式答辩不得把规划或预测口径改写为已实现事实。
- 三项快速签到已接入并通过模拟器交互闭环；S08-03 仍需按统一起点和计时口径正式验证一次签到是否可在 10 秒左右完成，当前人工点击顺畅不能替代计时证据。

## 下一步唯一任务

- S08-03：确保一次签到可在 10 秒左右完成。
