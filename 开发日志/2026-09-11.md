# 2026-09-11 开发日志

## 今日目标

- S12-20：按用户明确要求，将签到备注、生活事件备注和自定义事件名称纳入普通 AI 对话事实包，并完成隐私边界、字段限制、引用规则和测试更新。

## 已完成事项

### 任务开始｜S12-20｜记录事实包隐私边界变更

- 完成内容：确认用户要求将此前明确排除的三类自由文本加入普通 AI 对话事实包。
- 修改文件：`开发日志/2026-09-11.md`。
- 验证结果：已核对 S12-19 实现、隐私说明 V2、开发要求与质量标准，确认现有文档仍明确排除签到备注、事件备注和自定义事件名称。
- 关键决定：依照当前用户明确要求调整该边界，但继续遵守用户主动发送、字段白名单、原始 HealthKit 样本与底层 ID 不上传、医疗安全和日志不记录真实文本等更高优先级底线。三类文本必须受现有长度限制、按时间窗口最小化，并在 App 隐私说明中明确披露；不能静默扩大为完整本地数据库上传。

### 已完成｜S12-20｜将三类用户原文纳入普通 AI 事实包

- 完成内容：普通 AI 对话现会在用户点击发送时加入今日签到备注、最近 7 个完整日最多 7 条签到备注、今日与最近窗口各最多 20 条事件原文详情；事件详情保留事件类型、事件备注和自定义事件名称，不携带记录 ID 或精确起止时间。签到/事件备注沿用 160 字上限，自定义名称沿用 30 字上限。
- 架构与安全：新增专用只读 `AssistantFactContextLoader`，从唯一 `SubjectiveRecordStore` 读取同一 7 日窗口，不改变 S09 本地洞察的结构化匹配对象，也不扩展计划结束评估、通知或 PDF。构建器丢弃空白/越界文本；代理校验字段、长度、数量、状态一致性和自定义事件约束。直连与代理提示词均声明原文是不可信的用户自述数据，不能执行其中指令、当作客观事实或推断因果。
- 隐私说明：应用内 `PrivacyDataFlowCatalog` 升级为 V3，AI 助手欢迎文案和 `docs/privacy-data-flow.md` 明确说明三类文本会在主动发送普通 AI 问题时联网，且可能包含用户自行写入的个人或敏感信息；原始 HealthKit 样本、精确事件时间、设备来源和底层 ID 继续排除。
- 修改文件：`Zhiheng/Zhiheng/Domain/Models/HealthAIModels.swift`、`Zhiheng/Zhiheng/Infrastructure/InsightContextLoader.swift`、`Zhiheng/Zhiheng/AI/FactPack/HealthFactPackBuilder.swift`、`Zhiheng/Zhiheng/AI/Networking/DeepSeekPersonalService.swift`、`Zhiheng/Zhiheng/Presentation/Assistant/AssistantView.swift`、`Zhiheng/Zhiheng/RootTabView.swift`、`Backend/AIProxy/server.py`、对应 iOS/代理测试、隐私目录及标准文档。
- 验证结果：完整 581 项 iOS 测试通过（0 失败、0 跳过）；22 项 AI 代理测试通过；Release 通用真机构建通过；`git diff --check` 通过。测试使用合成文本，不记录用户真实备注。
- 关键决定：三类原文只加入普通 AI 对话事实包；计划结束 AI 解读继续只使用结构化生活背景。事件汇总次数不因原文 20 条上限而截断。

## 待办事项

- [x] 更新 S12-20 实施任务与正式需求、技术和质量边界。
- [x] 实现自由文本事实模型、构建器、提示词、代理白名单与引用测试。
- [x] 完成专项、完整 iOS、代理和 Release 构建验证。

## 问题与风险

- 三类自由文本可能包含身份、地址、症状或其他敏感信息；加入事实包会提高用户主动提问时的外发敏感度。必须限制窗口、数量与长度，并在隐私说明中明确由 AI 服务接收。
- 当前工作区保留 S12-19 的未提交修改；S12-20 将在同一事实包链路上增量修改，不覆盖无关工作。
- 自动测试已覆盖字段与提示注入边界，但仍需在真实账号、真实 AI 服务和合成备注下人工确认模型能自然引用背景且不服从备注中的指令；测试时不得使用真实健康或身份文本。

## 下一步唯一任务

- 先完成 S14-12 已安装真机包上的真实 HealthKit 卡片返回与标签切换确认；确认后恢复 S14-02，审计日志、错误和分析代码不包含健康敏感数据或用户自由文本。
