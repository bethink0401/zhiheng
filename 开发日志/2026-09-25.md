# 2026-09-25 开发日志

## 今日目标

- 准备将当前知衡项目提交到 GitHub，并把根目录 README 重写为面向仓库访客的项目介绍。

## 已完成事项

### 已完成｜GitHub 发布准备与 README 重写

- 确认远程仓库为 `https://github.com/bethink0401/zhiheng.git`，当前分支为 `main`。
- 新增根目录 `README.md`，改写为项目介绍，包含产品定位、核心闭环、功能能力、技术架构、隐私安全边界、开发运行方式、文档入口和当前状态。
- 将本地 `tmp/` 加入 `.gitignore`，避免把渲染截图、临时 PDF 和 QA 中间产物提交到 GitHub；未删除用户文件。
- 当前工作区还包含此前已有的代码、文档、素材新增/删除和 S14 演示模式改动，本次发布准备不覆盖或清理这些改动。

## 验证与待办

- 待执行：检查 README 与 Git 差异、确认待提交文件清单、运行与本次提交相匹配的静态检查，然后创建提交并推送到远程。
- 风险：当前工作区改动范围较大，推送前需要确认没有 API Key、真实健康数据或临时产物进入暂存区。

## 下一步唯一任务

- 完成 GitHub 提交前审计并推送 `main`。
