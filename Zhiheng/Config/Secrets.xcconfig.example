// Copy this file to Secrets.xcconfig only when an approved local override is needed.
// Secrets.xcconfig is ignored by version control.
// Never place an AI provider production key in the iPhone app configuration.
// Formal AI access must use the project's backend proxy.

// Add task-approved local overrides below. Keep this example file value-free.
