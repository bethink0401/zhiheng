# 2026-09-04 开发日志

## 今日目标

- S09-02 / REQ-08：由既有趋势引擎和共享健康快照生成确定、可追溯的洞察事实层。
- S09-03 / REQ-08：匹配同一当前窗口内的既有感受与生活事件，形成可重新校验的结构化背景候选。
- S09-04 / REQ-07、REQ-08：基于事实质量和同期背景缺口，确定性地产生零个或一个关键追问。
- S09-05 / REQ-08：在洞悉页以四层组合卡分开展示事实、可能解释、不确定性和建议，并安全承接最多一个追问。
- S09-06 / REQ-08：在四层卡内显示同一程序事实的时间范围、有效日、聚合值、变化门槛、同向日、数据质量、来源和规则版本。
- S09-07 / REQ-08：为洞察提供已读、忽略和同类不再提醒的本地状态，支持真实/演示隔离、失败保护和恢复。
- S09-08 / REQ-08：建立洞察低焦虑文案测试清单、固定回归矩阵和发布前专业审阅门禁。

## 已完成事项

### 开始｜S09-02｜事实层范围与安全边界

- 本轮只实现纯 Swift 本地事实生成器和测试，不修改今日双卡或洞悉页面，不读取主观 Store，不匹配生活事件，不生成可能原因/追问/建议，不新增持久化、网络或 AI 调用。
- 默认覆盖当前已有集中趋势阈值的六类指标；事实复用 7 天当前窗口、此前 28 天基线、稳健中位数、变化幅度、同向日、孤立值保护、来源稳定性和算法版本，不复制趋势算法或上传原始样本。
- 权限未请求、系统不可用、无可见数据、查询失败、快照未加载、90 天读取范围不足、同日来源冲突、最近完整日缺失和未配置趋势指标分别保留，不补零、不把缺失解释为恶化。
- 固定指标、真实/演示模式、时区、分析截止日和生成器版本形成稳定事实 ID；同日刷新更新同一事实身份，跨日、跨模式或规则升级形成新身份。当前任务标记 `[~]`，验证通过前不标完成。

### 测试中｜首次专项构建失败

- 首次运行洞察专项测试时，Swift 编译器指出新测试夹具用 `??` 包装可抛错的样本构造，外层没有显式 `try`；测试因此在编译阶段取消，未执行任何用例，不能计为通过。
- 已将夹具改为显式 `if` 分支，仅测试文件受影响；保留本条失败记录并重新运行同一专项命令。此问题没有进入 App 运行路径，也不涉及真实数据。

### S09-02｜趋势事实层完成

- 完成内容：新增纯 Swift `InsightFactGenerator` 和 `InsightFactSet`。生成器只消费 Root 已有共享快照、读取范围、访问状态、模式、参考时刻和时区，默认按六类集中趋势阈值生成截至今日之前的 7 天当前窗口与此前 28 天基线事实；复用既有趋势引擎，不另算一套趋势。
- 事实依据：逐项保留有效日、聚合中位数、相对变化、配置/实际门槛、同向日、孤立异常保护、所选来源和阈值版本。未申请、系统不可用、无可见数据、查询失败、未加载、窗口不足、同日来源冲突、跨日来源变化、最近完整日断档、未配置指标和分析无法形成分别降级；查询完成不冒充逐项授权，缺失不补零。
- 身份与隐私：使用 `s09-fact-generator-v1`、指标、分析截止日、时区和真实/演示模式生成 RFC 9562 version 8 稳定 UUID；相同日刷新及样本顺序/UUID改变不产生重复事实，跨日/模式/时区/规则版本改变会形成新身份。输出不含原始样本数组或样本 ID，不读取主观记录、不存储、不联网、不调用 AI。
- 专项验证：新增 **19 项**事实生成测试，加上 S09-01 的 24 项，共 43 项专项通过。覆盖六类默认指标、35 个完整公历日、稳定/持续升降、当前/基线不足、间歇断档与最近日缺失、孤立异常、访问与读取状态、范围不足、错误指标/窗口、Apple 设备优先、同日来源冲突、跨日来源变化、固定身份、真实/演示隔离及夏令时。
- 完整验证：最终结果包 `Test-Zhiheng-2026.09.04_11-16-49-+0800.xcresult` 为 **364 项 iOS 测试通过、0 失败、0 跳过**；14 项 AI 代理测试通过。Debug 测试构建与 Release 通用真机无签名构建成功；Xcode 工程格式、敏感边界静态检索和 `git diff --check` 通过。
- 测试边界：全部新增数据为合成 Fixture；本轮没有 UI 改动，因此未执行或宣称真机、页面或真实健康含值验收。首次专项构建失败已如实保留，上述通过数字来自修正后的最终结果包。
- 修改文件：新增 `Zhiheng/Zhiheng/Domain/Models/InsightFactGenerator.swift`、`Zhiheng/ZhihengTests/InsightFactGeneratorTests.swift`；扩充 `InsightDataUnavailableReason`；登记 Xcode 工程，并同步 `AGENTS.md`、README、需求、技术设计、执行步骤、实施总方案及本日志。未新增第三方依赖、存储 Schema、网络请求或秘密配置。
- 状态：S09-02 标记 `[x]`。事实只说明程序观察，不含生活背景或因果解释；唯一下一步为 S09-03 匹配同期生活事件和主观感受。

## 待办事项

- [x] S09-02：事实生成、边界测试和完整回归。
- [x] S09-03：同期主观背景匹配、隐私/时区/失败保护、失效校验和完整回归。
- [x] S09-04：最多一个关键追问、质量/模式守门、同窗口去重和完整回归。
- [x] S09-05：四层洞察卡、页面接入、大字号适配和完整回归。
- [x] S09-06：洞察数据依据/来源展示、降级状态和完整回归。
- [x] S09-07：洞察已读、忽略、不再提醒、恢复控制和完整回归。
- [x] S09-08：编写低焦虑文案测试清单、自动文案回归和审阅边界。
- [ ] S10-05：建立洞察到计划候选的规则映射。

## 问题与风险

- 四层卡及其数据依据已进入洞悉页，但 S09-03 的同期记录仍只表示时间重叠，不能证明某个事件解释了某项变化。
- S09-07 用户状态已经本地持久化并可恢复；同类提醒偏好目前只形成可靠本地输入，需等 S13 实际通知调度接入后才能验证通知过滤效果。
- S09 固定文案已完成工程与产品侧低焦虑/医疗边界检查；医学专业人士正式审阅仍是发布前门禁，不能把本轮自动测试当作专业签字。零个或一个追问当前仍为可选展示，没有回答入口。
- 按用户要求本轮不做新的大字体专项调优；模拟器不能替代真实健康含值与真机 VoiceOver 的最终人工验收。

## 下一步唯一任务

- S10-05：建立洞察到计划候选的本地确定规则映射；只从质量合格事实选择已有低风险模板，不自动开始计划，不让 AI 替代规则。

## 继续开发

### 开始｜S09-03｜同期主观背景匹配

- 范围：只匹配 S09-02 事实集当前 7 个完整本地日，不把此前 28 天基线中的主观记录混入当前背景。
- 边界：真实与演示模式严格隔离；只保留结构化评分、事件种类、强度、时间范围及稳定 ID/更新时间引用，不复制感受备注、事件备注或自定义名称。
- 语义：匹配结果仅表示“同期候选背景”，不绑定到某项客观指标，不生成原因、追问、建议、页面、持久化或 AI 上传。
- 保护：读取失败不返回部分结果；时区不一致拒绝合并；后续复用前重新读取，以识别新增、编辑、删除或移出窗口的记录。

### 测试中｜首次日期断言与代理命令路径

- 首次运行 15 项 S09-03 定向测试时，14 项通过，1 项失败。失败来自测试夹具把预期的 `2026-08-28` 写成 `2026-09-28`；实际匹配窗口为正确的最近 7 个完整本地日。修正该断言后同一专项 15 项全部通过，首次失败未隐藏。
- 首次从仓库根目录执行代理 `unittest` 时，测试模块因工作目录中找不到 `server` 而未加载，不能计为测试结果；改在 `Backend/AIProxy` 目录按项目路径运行后 14 项全部通过。

### S09-03｜同期主观背景匹配完成

- 完成内容：新增 `InsightContextMatcher`、`InsightContextWindow`、两类结构化匹配记录及只读 `InsightContextLoader`。匹配窗口严格从 S09-02 的规范 35 日事实范围派生，只覆盖末尾最近 7 个完整本地日，夏令时按公历日处理；不会把此前 28 日基线中的主观记录当作当前背景。
- 数据边界：真实模式逐日读取既有签到，再读取与半开窗口相交的生活事件；演示模式不访问真实 Store，纯匹配规则也拒绝把外部记录重新标成演示数据。输出保留三项评分、事件种类/强度/起止时间和带类型的稳定 ID、更新时间、模式，不复制感受备注、事件备注或自定义名称，不新增主观数据副本。
- 可靠性：七次签到读取和一次事件读取按全有或全无发布；任一读取失败、签到日期/时区不同、冲突 ID、同日多签到或损坏记录均不返回部分结果。结果确定排序并去除完全重复记录；签到和事件可安全复用相同 UUID，因为引用类型参与区分。
- 失效保护：`validate` 使用同一事实集重新读取完整匹配并比较；新增、编辑、删除或事件移出窗口都会把旧快照标为过期，读取失败不会被误报成空记录。
- 语义与隐私：匹配对象不保存支持事实 ID，因此只表示共享时间窗口，不会自动把某个背景绑定到某项指标，也不生成解释、追问、建议、页面、Schema、持久化、网络请求或 AI 上传。
- 专项验证：新增 **15 项**测试，覆盖 7 日/35 日边界、DST、半开事件范围、结构化字段和自由文本剥离、稳定排序/去重、冲突记录、跨种类同 UUID、非法事实范围、空记录、演示零读取、时区错配、读取失败、重新校验及客观数据不可用时仍尊重主观记录。
- 完整验证：最终结果包 `Test-Zhiheng-2026.09.04_12-27-15-+0800.xcresult` 为 **379 项 iOS 测试通过、0 失败、0 跳过**；14 项 AI 代理测试通过；最终模式保护加入后 Release 通用 iPhone 无签名增量构建成功。`plutil` 工程检查和 `git diff --check` 通过。
- 测试边界：新增用例全部使用合成记录，不含真实健康数据或私人文本。本轮没有 UI 改动，因此未执行或宣称新的真机/页面验收。
- 修改文件：新增 `Zhiheng/Zhiheng/Domain/Models/InsightContextMatching.swift`、`Zhiheng/Zhiheng/Infrastructure/InsightContextLoader.swift`、`Zhiheng/ZhihengTests/InsightContextMatchingTests.swift`；登记 Xcode 工程，并同步 `AGENTS.md`、README、需求、技术设计、执行步骤、实施总方案及本日志。未新增依赖、存储迁移、网络或秘密配置。
- 状态：S09-03 标记 `[x]`。唯一下一步为 S09-04 实现“最多一个关键追问”的规则。

## 继续开发｜S09-04

### 开始｜关键追问规则与安全边界

- 本轮只实现纯 Swift 本地决策规则和测试，不修改页面，不生成可能解释或建议，不新增 SwiftData Schema、网络请求、AI 调用或自由文本上传。
- 只有质量可解释的趋势或严格的主客观不一致、且同期背景确有缺口时才允许提问；已有结构化背景时不猜测它能解释某项指标，也不继续追问原因。
- 追问结果必须是零个或一个，并保留稳定问题 ID、触发种类、支持事实与既有记录引用；相同分析窗口可由调用方传入已处理 ID 以阻止重复询问。
- 演示模式不向真实用户提问；数据不足、来源变化、单日异常保护或没有明确背景缺口时保持不提问。验证完成前任务保持 `[~]`。

### S09-04｜最多一个关键追问完成

- 完成内容：在既有洞察领域文件中新增 `InsightFollowUpQuestionRule`、单问题值对象和“提问/不提问”决策。规则对同一事实集和同期匹配一次只返回零个或一个问题；问题保留规则版本、支持事实 ID 和既有结构化引用，不把用户未回答内容写成事实。
- 触发顺序：质量合格的持续变化或达到实际门槛的“值得继续观察”存在时，没有签到先询问精力/压力/身体感受；已有签到但没有生活事件时只询问一项生活背景；两类背景都有时不继续追问。若四类核心指标均质量合格且未见明确变化，而结构化签到出现低精力、较高压力或较差身体感受，可在缺少生活事件时补一个背景问题，但不以客观数据否定感受。
- 守门与去重：演示模式、有效日不足、来源不稳定/非单一、孤立异常保护、弱变化、核心事实不完整或无高价值缺口时返回不提问。规则版本、问题种类、模式、时区和分析截止时间生成稳定 RFC 9562 version 8 UUID；调用方传入同窗口已回答/忽略 ID 后返回 `alreadyHandled`，本步不新增处理状态持久化。
- 隐私与安全：输出不复制感受备注、事件备注或自定义名称，不读取原始 HealthKit 样本，不访问 Store、网络、AI 或 CareKit，也不生成可能解释、建议和因果结论。固定问句仍需 S09-08 医学与低焦虑文案审阅。
- 专项验证：新增 **10 项**追问规则测试；`InsightTests` 共 **34 项**全部通过。覆盖变化时的主观/生活背景优先级、已有背景不追问、事件存在但签到缺失、严格主客观不一致、舒适感受与核心事实不完整、达到门槛的观察项、弱变化/孤立异常/读取失败、演示隔离、稳定 ID/已处理抑制，以及模式与窗口错配拒绝。
- 完整验证：结果包 `Test-Zhiheng-2026.09.04_16-23-58-+0800.xcresult` 为 **389 项 iOS 测试通过、0 失败、0 跳过**；14 项 AI 代理测试通过；Release 通用 iPhone 无签名构建成功。`plutil` 工程检查和 `git diff --check` 通过。首次在受限沙箱列出 Xcode 工程时因模拟器与缓存目录权限失败，获准在正常构建环境重跑后成功；该环境失败未计为代码测试结果。
- 测试边界：新增用例全部使用合成事实与结构化记录，不含真实健康数据或私人文本。本轮没有 UI 改动，规则尚未显示在 App 中，因此未执行或宣称新的页面/真机验收。
- 修改文件：扩充 `Zhiheng/Zhiheng/Domain/Models/Insight.swift` 与 `Zhiheng/ZhihengTests/InsightTests.swift`，并同步 `AGENTS.md`、README、需求、技术设计、执行步骤、实施总方案及本日志。未新增依赖、工程源文件引用、存储迁移、网络或秘密配置。
- 状态：S09-04 标记 `[x]`。唯一下一步为 S09-05 实现事实、可能解释、不确定性、建议四层卡片。

## 继续开发｜S09-05

### 开始｜四层卡片范围与安全边界

- 本轮只实现一个可见的洞察组合卡及其纯 Swift 展示映射，四层固定为“观察到的事实、可能解释、不确定性、建议”；不新增第二套趋势算法、SwiftData Schema、AI 请求、通知或计划自动创建。
- 事实继续来自 S09-02 的 7/28 天程序结果；可能解释只能引用 S09-03 的结构化同期背景，并明确“可能相关、不代表因果”。缺少背景时承接 S09-04 的零个或一个追问，不自行追加问题。
- 主观记录读取失败不得伪装成空背景；演示模式不读取真实 Store。数据不足、来源变化、孤立异常或没有可靠变化时展示保守的继续观察状态，不生成强结论。
- S09-06 的详细数据依据/来源、S09-07 的已读/忽略持久化及 S09-08 的正式医学文案审阅不在本轮范围。验证完成前 S09-05 保持 `[~]`。

### 测试中｜编译与模拟器环境问题

- 首次运行 S09-05 定向测试时，新增展示工厂中的两个可选 `if` 表达式缺少显式类型，Swift 编译报错并在执行用例前停止；补充 `InsightFollowUpQuestion?` 和 `String?` 类型后重跑通过，未把该次编译失败计为测试通过。
- 第一次最大辅助字号截图发现标题与“演示数据”标记在同一横排内被挤压；卡片改为辅助字号下纵向头部、正文整宽布局后重新安装并截图，滚动内容无横向裁切。其间定向测试关闭了模拟器，首次重装复验因设备处于 shutdown 失败；重新启动同一设备后复验成功，该环境中断不属于 App 功能失败。
- 完整回归和 Release 首次在受限沙箱运行时，CoreSimulator 服务不可访问且独立构建目录需要重新解析已锁定 Swift 包，命令在测试开始前失败；获准访问 Xcode 模拟器及本机包缓存后按原命令重跑成功，未变更依赖版本。

### S09-05｜事实、可能解释、不确定性、建议四层卡片完成

- 完成内容：新增 `InsightFourLayerCardPresentation`、`InsightCardContextState` 与纯 Swift `InsightFourLayerCardFactory`，固定且只输出“观察到的事实、可能解释、不确定性、建议”四层。一次优先选择一个质量合格的持续变化，其次选择达到实际门槛的观察项；完整稳定集、数据不足、来源变化、孤立异常和读取失败分别保守处理。
- 页面接入：`InsightsView` 在洞悉页顶部接入组合卡，Root 注入与每日签到共用同一 `SubjectiveRecordStore` 的 `InsightContextLoader`。真实模式读取 S09-03 匹配，演示模式零读取；共享快照、访问状态、数据模式、进入页面或下拉刷新时重建展示。标准字号完整显示，大辅助字号改为纵向头部和整宽正文，卡片保留滚动与自然增高。
- 事实与解释边界：事实展示当前 7 天和此前 28 天的聚合中位数、相对变化与趋势状态；生活事件只使用已剥离自由文本的事件种类，明确写成同期可能相关、不代表因果。严格主客观不一致尊重用户感受；上下文读取失败不会冒充“没有背景”并触发问题。
- 追问与隐私：卡片只承接 S09-04 的零个或一个本地规则问题，标明回答可选且未回答内容不是事实；演示模式不询问真实用户。不读取或上传原始 HealthKit 样本、备注和自定义名称，不调用 AI、不新增网络请求/SwiftData Schema，也不自动创建 CareKit 计划。
- 专项验证：新增 **9 项** S09-05 测试，`InsightTests` 最终 **43 项**通过；覆盖四层顺序、一个变化优先级、事件非因果、严格主客观不一致、稳定全量、数据不足、读取失败、演示隔离、真实/演示模式拒绝及最多一个追问。
- 完整验证：结果包 `Test-Zhiheng-2026.09.04_16-51-10-+0800.xcresult` 为 **398 项 iOS 测试通过、0 失败、0 跳过**；14 项 AI 代理测试通过；Release 通用 iPhone 无签名构建成功。`plutil` 工程检查与 `git diff --check` 通过。
- 人工验证：标准字号截图 `/private/tmp/s09-05-insight.png` 显示完整四层演示卡；修复后的最大辅助字号截图 `/private/tmp/s09-05-insight-axxxl-fixed.png` 显示头部不再挤压、正文可滚动且无横向裁切。未宣称真实 HealthKit 含值或真机人工验收。
- 修改文件：扩充 `Insight.swift` 与 `InsightTests.swift`，修改 `RootTabView.swift`、`InsightsView.swift`，并同步 `AGENTS.md`、README、需求、技术设计、执行步骤、实施总方案及本日志。未新增依赖、工程源文件引用、存储迁移、网络或秘密配置。
- 状态：S09-05 标记 `[x]`。唯一下一步为 S09-06 显示洞察的数据依据和来源。

## 继续开发｜S09-06

### 开始｜数据依据与来源范围

- 本轮只在 S09-05 四层卡内增加可展开的“数据依据与来源”，直接映射 S09-02 事实已经保存的 7/28 天范围、有效日、中位数、相对变化、实际门槛、同向日、孤立异常保护、来源稳定性和版本；不重新计算趋势，不读取或展示原始样本。
- 主要变化卡只展示该项变化的依据；“四项核心指标未见明确变化”显示四项核心依据；完整稳定集或证据不足显示本卡实际引用的全部事实，确保页面结论与依据一一对应。
- 来源只显示面向用户的 `Apple Watch`、`iPhone` 或既有来源名称，不显示 bundle identifier、设备序列或原始记录 ID。不可用、来源冲突/变化、读取失败和窗口不足分别说明，不用 0 或“正常”占位。
- 用户本轮明确不要求继续处理大字体。本任务不做新的最大字号专项改版或截图验收，但保留现有系统字体、滚动容器和自然换行，不主动破坏已有可访问性。
- S09-07 的已读/忽略状态和 S09-08 的医学文案审阅不在本轮范围。验证完成前 S09-06 保持 `[~]`。

### 测试中｜首次依据顺序断言失败

- 首次运行 S09-06 的 49 项 `InsightTests` 时 47 项通过、2 项失败；失败来自新增测试把多指标依据顺序写成阈值目录顺序，而展示工厂按全项目 `HealthMetricType.allCases` 的既定规范顺序输出。数值、时间范围、来源脱敏、质量说明和版本断言均通过。
- 已把测试期望改为事实生成器实际使用的规范顺序：步数、睡眠、静息心率、HRV、活动能量、运动分钟。实现无需改动；本次失败不计为通过，修正后按原命令重跑。

### S09-06｜数据依据与来源完成

- 完成内容：在 `InsightFourLayerCardPresentation` 中新增只读 `InsightEvidencePresentation`，由同一个 `InsightFactSet` 生成。主要变化只带该项事实；严格主客观不一致带四项核心事实；稳定集或证据不足带结论实际使用的全部事实，按项目既定指标顺序展示。
- 展示内容：洞悉四层卡新增默认收起的“数据依据与来源”，展开后逐项显示当前 7 日与此前 28 日的本地日期范围、有效日/期望日、中位数、相对变化、实际门槛、同向日/最低要求、来源、数据质量、趋势规则与事实生成版本；可随页面完整滚动。
- 数据与隐私：所有内容直接来自 S09-02 聚合事实，不重算趋势、不读取原始 HealthKit 样本。来源只使用去重后的 `displayName`，不展示 bundle identifier、设备序列、样本 UUID、主观记录 ID、备注或自定义标签；不可用事实不伪造中位数、来源、变化或 0。
- 降级分支：尚未授权、系统不可用、无可见数据、读取失败、未加载、窗口不完整、来源冲突/变化、最近断档、未配置指标、分析不可用、当前/基线有效日不足和孤立异常保护均保留独立说明，不把质量问题写成健康恶化。
- 专项验证：新增 **6 项** S09-06 测试；`InsightTests` 最终 **49 项**全部通过。覆盖主要变化的窗口/数值/门槛/同向日/来源/版本、四项主客观不一致依据、六项稳定集依据、无数据不补 0、来源变化只显示可读名称以及孤立异常保护。
- 完整验证：结果包 `Test-Zhiheng-2026.09.04_19-55-27-+0800.xcresult` 为 **404 项 iOS 测试通过、0 失败、0 跳过**；14 项 AI 代理测试通过；Release 通用 iPhone 无签名构建成功。`plutil` 工程检查与 `git diff --check` 通过。
- 人工验证：iPhone 17 Pro / iOS 26.5 模拟器普通字号下，演示洞察可展开依据并显示步数 7/28 日范围、7/7 与 27/28 有效日、6,040/5,200 中位数、+16%、15% 门槛、5/7 同向日、演示来源、质量与两个规则版本；滚动后底部时区说明完整可见。截图为 `/private/tmp/s09-06-evidence.png`。按用户明确要求，本轮未做新的最大字号专项调优或验收；现有系统字体、换行和滚动能力未移除。
- 修改文件：扩充 `Insight.swift`、`InsightsView.swift` 与 `InsightTests.swift`，并同步 `AGENTS.md`、README、需求、技术设计、执行步骤、实施总方案及本日志。未新增依赖、工程文件引用、Schema、网络、AI 或秘密配置。
- 状态：S09-06 标记 `[x]`。唯一下一步为 S09-07 实现洞察已读、忽略和不再提醒。

## 继续开发｜S09-07

### 开始｜洞察状态与用户控制边界

- 本轮只实现当前洞察的已读/未读、本期忽略/恢复，以及按洞察主题关闭/恢复同类低频提醒；不改变趋势、四层结论、追问内容、AI、CareKit 或 S13 通知调度。
- 当前洞察使用事实生成版本、模式、时区、分析窗口、实际依据事实、结构化背景版本和可选问题形成稳定散列 ID；同类提醒按“具体指标变化、感受与数据对照、稳定概览、数据质量观察”分组。SwiftData 只保存散列后的洞察 ID、主题键、真实/演示模式和操作时间，不保存原始健康样本、来源标识、备注或主观记录 ID。
- 新建独立 `ZhihengInsightInteractions` 版本化 Schema V1，不修改主观记录 Schema V1。忽略仅隐藏本期卡片并提供恢复；不再提醒不隐藏洞悉页内容，预留给 S13 的低频提醒过滤。任何读取/写入失败均保留卡片和原状态，不显示假成功。

### 测试中｜首次专项构建失败

- 首次运行 S09-07 洞察专项测试时，App 目标代码已编译，但新增测试文件遗漏 `@testable import Zhiheng`，测试目标在执行用例前因找不到洞察交互类型而停止；本次没有测试用例执行，不能计为通过。
- 已补充测试模块导入，将使用同一目标和命令重新运行。此前受限沙箱无法访问 CoreSimulator 与 Swift 包缓存，只完成工程格式和差异检查；该环境失败同样不计为代码测试结果。
- 第二次专项构建同样在执行用例前停止：迁移计划测试使用 `XCTAssertEqual` 比较协议元类型，Swift 6 不接受该 `Equatable` 约束。实现目标继续编译通过；已改用项目现有测试采用的显式元类型布尔比较，继续按原范围重跑。

### S09-07｜洞察状态与用户控制完成

- 完成内容：四层卡新增“未读/已读”“标为已读/未读”“忽略本期”和按主题“不再提醒/恢复提醒”。忽略后只把当前窗口卡片替换成紧凑提示，并可恢复；同类提醒设置只面向未来低频提醒，不删除健康/主观数据，也不阻止洞悉页继续展示内容。
- 身份与存储：`InsightFourLayerCardFactory` 使用展示/事实版本、模式、时区、分析窗口、实际依据、可选问题和结构化背景生成稳定 RFC 9562 version 8 散列 UUID。新增独立 `InsightInteractionsSchemaV1` 与显式迁移计划，只保存散列洞察 ID、主题、模式和操作时间，真实/演示隔离，不修改主观记录 Schema V1，不保存原始 HealthKit 样本、自由文本、底层来源或主观记录 ID。
- 失败保护：读取失败仍展示洞察正文，但禁用控制并提供重试；写入失败回滚持久化上下文、保留原 UI 状态，不乐观显示成功。清除最后一个当前状态时删除空实体；恢复同类提醒时删除对应主题偏好。
- 自动验证：新增 **11 项**测试，其中身份 2 项、Store/Session 9 项；覆盖稳定与跨模式/主题身份、显式 Schema/迁移、已读与忽略独立撤销、主题跨窗口生效、跨主题/模式隔离、恢复、磁盘重建、损坏记录拒绝，以及读写失败不显示假成功。`InsightTests` 与 `InsightInteractionStoreTests` 定向共 **60 项**通过，结果包 `/tmp/zhiheng-s09-07-targeted-final.xcresult`。
- 完整验证：最终结果包 `/tmp/zhiheng-s09-07-full-2.xcresult` 汇总为 **415 项 iOS 测试通过、0 失败、0 跳过**；AI 代理 **14 项**通过；Release 通用 iPhone 无签名构建成功。工程格式、隐私边界静态检索和 `git diff --check` 通过。
- 人工验证：iPhone 17 Pro / iOS 26.5 模拟器普通字号下完成未读→已读、关闭同类提醒、忽略本期、重启后状态保持、恢复本期、恢复提醒及恢复未读的完整操作；最后保留默认未读且提醒开启。截图为 `/private/tmp/s09-07-insight-controls.png`。按用户明确要求，本轮没有新增大字体专项调优或验收。
- 修改文件：新增 `InsightInteractionStore.swift` 和 `InsightInteractionStoreTests.swift`；扩充 `Insight.swift`、`InsightsView.swift`、`RootTabView.swift`、`InsightTests.swift` 与 Xcode 工程登记，并同步 `AGENTS.md`、README、需求、技术设计、执行步骤、实施总方案及本日志。未新增第三方依赖、网络、AI、CareKit 或通知调度。
- 状态：S09-07 标记 `[x]`。唯一下一步为 S09-08 编写低焦虑文案测试清单。

## 继续开发｜S09-08

### 开始｜低焦虑文案验收边界

- 本轮建立可重复执行的 S09 洞察文案清单，覆盖事实、可能解释、不确定性、建议、可选追问、数据依据/质量降级及已读/忽略/提醒控制；不修改趋势算法、数据范围、提醒调度、AI 或 CareKit。
- 自动回归负责固定禁用断言、四层完整、最多一个中性追问、主观感受不被设备否定、数据不足不形成强结论、数值方向不等同健康好坏及孤立日期中性表达。
- 工程与产品侧可以依据现有医疗安全底线完成发布前检查，但不能冒充医学专业人士签字；医学专业审阅、真实用户焦虑反馈和真机 VoiceOver 仍作为独立发布/用户测试门禁如实保留。

### S09-08｜低焦虑文案测试清单完成

- 清单基线：新增 `docs/insight-low-anxiety-copy-checklist.md`，固定事实、可能解释、不确定性、建议、追问、依据/质量和用户控制七类职责；列出八项一票否决规则、COPY-01～COPY-18 场景矩阵、自动与人工复验分工、审阅记录及必须重跑的变更触发条件。
- 文案收口：两个生活背景问题把“身体不适”改为中性的“身体感受”，仍只询问是否存在变化，不预设原因；离群证据把“单日异常保护/异常日期”改为“孤立日期保护/该日期”，避免把统计保护误读为身体异常。趋势、事实、阈值、追问触发条件和建议动作均未改变。
- 自动验证：新增 **5 项**文案测试，覆盖变化/同期事件/稳定/主客观不一致/不可用/孤立日期/演示分支的禁用断言与四层完整，三类中性可选追问，数值方向不等同健康好坏，全部 11 类不可用原因不产生强结论或伪造 0，以及孤立日期中性表达。`InsightTests` 专项 **56 项**全部通过，结果包 `/tmp/zhiheng-s09-08-targeted.xcresult`。
- 完整验证：结果包 `/tmp/zhiheng-s09-08-full.xcresult` 汇总为 **420 项 iOS 测试通过、0 失败、0 跳过**；AI 代理 **14 项**通过；Release 通用 iPhone 无签名构建成功。Xcode 工程格式与 `git diff --check` 通过。
- 审阅边界：工程自动回归、产品低焦虑逐项检查和医疗边界工程检查通过；没有诊断、用药、治疗承诺、确定因果或持续监护承诺。医学专业人士审阅仍标记待完成，是正式公开发布前门禁；真实用户焦虑反馈留给 S15，不虚构结果。
- 测试边界：本轮只修改固定文字和测试清单，未改变页面结构，因此未新增模拟器视觉或真机验收；按用户要求不做新的大字体专项调优。现有真实健康含值和真机 VoiceOver 风险保持不变。
- 修改文件：新增低焦虑文案清单；扩充 `InsightTests.swift`，调整 `Insight.swift` 的固定问句与孤立日期说明，并同步 `AGENTS.md`、README、需求、技术设计、质量标准、执行步骤、实施总方案及本日志。未新增依赖、Schema、网络、AI、CareKit 或通知调度。
- 状态：S09-08 标记 `[x]`，S09 可解释洞察阶段完成。唯一下一步为 S10-05 建立洞察到计划候选的规则映射。

## 继续开发｜S10-05

### 本轮目标

- 建立纯 Swift、本地确定性的洞察到低风险微计划候选规则，一次最多返回一个既有白名单模板。
- 复用 S09 的有效日、稳定单一来源、孤立日期保护和结构化背景校验；数据不足、来源变化、读取失败、待补背景或无安全匹配时不提供候选。
- 本轮只生成候选，不创建 CareKit 计划、不自动开始行动、不调用 AI，也不增加新的健康阈值或原始数据存储。

### 开始状态

- `docs/implementation-plan-v1.md` 中 S10-05 已标记为 `[~]`。
- 现有十类 `MicroPlanTemplateLibrary` 继续作为唯一模板白名单；用户确认和 CareKit 创建仍沿用既有 S10-06 边界。
