# AGENTS.md

本文件是“知衡”项目中所有开发代理和协作者必须遵守的项目级指令。进入项目后先阅读本文件，再开始分析、设计、编码、测试或修改文档。

## 1. 项目概况

项目名称：知衡（暂定）

产品定义：面向全社会成年用户的 iPhone AI 健康洞察与行动助手。应用读取用户授权的 Apple Health/Apple Watch 数据，建立个人基线，对照用户的主观感受和生活情境，发现可解释的趋势，推荐一个低风险微计划，并通过执行结果沉淀“我的有效方法”。

产品定位：健康管理和生活方式改善工具，不是疾病诊断、治疗、处方或持续急救监护软件。

当前状态：

- 产品方案 V1.1 已完成。
- 实施方案 V1.3 已完成。
- S00 项目范围与文档基线已完成。
- 每日开发日志与标准文档体系已建立。
- S02 开源参考研究与 CareKitStore 技术样例已完成主要验收。
- SwiftUI iPhone 工程、基础领域边界、单元测试和五标签导航已建立。
- HealthKit 原六类真机授权、十六类映射、90 天有效日、28 天个人基线、通用 7 天窗口、稳健变化与覆盖率、单日异常值保护、三档趋势、集中阈值、来源规则、共享数据会话及详情依据均已完成；今日页、洞悉页和今日右上角设置使用同一数据边界，数据不足、来源变化或演示模式不会伪装成真实结论。
- 每日精力、压力、身体感受和 11 类生活事件已建立本地 SwiftData 闭环，支持用户主动记录、修改、删除、历史、7 天时间线、草稿/失败保护、模式隔离和重启恢复；进入 App 不再自动弹出感受窗口，原感受与数据对照引擎继续保留测试但不再提供页面入口。
- S09 可解释洞察已完成确定事实、结构化背景、最多一个追问、四层卡、依据来源、用户控制和低焦虑文案门禁；S10 已完成本地单一低风险计划候选及计划期结构化生活背景评估。AI 对话支持 DeepSeek Responses API 真实 SSE 流式回答，只消费最小化聚合事实包。
- CareKit 微计划已完成十类模板、确认创建、计划前聚合基线、进度、暂停/恢复、每日完成/跳过与可选反馈、提前结束、动态趋势、本地结束评估、可选 AI 解读、历史和单计划完整删除。旧计划、任务、Outcomes 与 SwiftData 基线的职责边界保持明确。
- S11-01～S11-09 已完成：方法卡由 CareKit 已结束历史动态生成，显示真实执行次数、加权完成率、四级数据质量、版本化四档可信度，以及独立的客观/主观变化；用户可经二次确认从来源模板创建 ID 独立的新计划与新基线继续验证，也可通过独立本地展示偏好隐藏并恢复方法。支持按睡眠、精力、压力和可坚持性筛选已有证据，缺失证据不伪装为没有变化。“我的有效方法”已按用户视觉参考重组为独立胶囊筛选与大圆角方法卡，保留原图标并统一操作按钮尺寸。以上路径均不改写旧历史，演示和读写失败有保护，不调用 AI 作决定。
- S13-01～S13-08 已完成：通知系统状态通过独立边界读取，今日右上角设置先展示用途和安全边界，只有用户明确确认后才请求系统权限；每日感受、微计划与低频趋势提醒均默认关闭。微计划只消费 CareKit 事实；低频趋势只消费本地质量合格的 7/28 天持续变化并服从 S09-07 同类提醒偏好，同一指标与方向不重复。三类提醒共享全部关闭、22:00～08:00 默认安静时间和 1/2/7 天最小间隔；优先级保持微计划、低频趋势、每日感受，公开通知内容统一为“知衡提醒 / 打开知衡查看”，不透露提醒类别或敏感负载，AI 不参与提醒选择。设置中可预览并导出最近 7 个完整日的健康摘要；PDF 默认匿名，只有隐私确认后才生成受保护的临时文件并打开系统分享，不上传、不调用 AI、不自动创建计划，应用侧临时副本会自动清理。
- S14-01 已完成：App 内“今日右上角设置 → 隐私与安全”提供可读的隐私说明与五条真实数据流，明确本机、Apple 系统、加密网络和外部接收方边界；只有用户主动发送 AI 问题或确认分享 PDF 时，完成动作所需的最小内容才会离开知衡。
- 当前通过 575 项 iOS 测试、15 项代理测试、模拟器实际界面检查、PDF 逐页渲染检查、Debug 测试构建和 Release 通用真机构建。后续唯一任务为 S14-02：检查日志、错误和分析代码不包含健康敏感数据。

## 2. 开始工作前必须阅读

按以下顺序阅读：

1. `AGENTS.md`：项目级强制规则。
2. `docs/README.md`：文档导航和当前状态。
3. `开发日志/` 中日期最新的日志：了解当天进度、风险、待办和唯一下一步。
4. `docs/execution-steps.md`：确认当前只应该推进哪一个小任务。
5. `docs/development-requirements.md`：确认功能需求和验收边界。
6. `docs/technical-design-spec.md`：确认架构和数据职责。
7. `docs/quality-security-standards.md`：确认测试、安全、隐私和完成标准。
8. `docs/implementation-plan-v1.md`：查看 S00～S16 总清单和任务状态。
9. `docs/project-plan-v1.md`：需要理解产品、比赛叙事或完整背景时阅读相关章节。

### 标准文件路径与职责

| 路径 | 负责回答的问题 | 何时更新 |
| --- | --- | --- |
| `docs/development-requirements.md` | 第一版要做什么、什么算验收通过 | 功能范围或验收要求变化时 |
| `docs/technical-design-spec.md` | 系统如何分层、数据放哪里、框架怎样协作 | 架构、数据模型、依赖或技术路线变化时 |
| `docs/execution-steps.md` | 当前先做什么、一次做多少、阶段如何放行 | 优先级、阶段门禁或下一任务变化时 |
| `docs/quality-security-standards.md` | 质量、安全、隐私和测试必须达到什么标准 | 安全边界、测试标准或发布要求变化时 |
| `docs/implementation-plan-v1.md` | S00～S16 全量任务与状态 | 任务新增、开始、完成或阻塞时 |
| `docs/project-plan-v1.md` | 产品愿景、用户、差异化和比赛方案 | 产品战略发生明确变化时 |
| `开发日志/README.md` | 每日日志如何创建和填写 | 日志制度变化时 |
| `开发日志/YYYY-MM-DD.md` | 当天实际完成事项、待办、风险和下一步 | 每完成一项实质工作后立即更新 |
| `docs/development-log.md` | 每日日志制度建立前的历史记录 | 历史归档，原则上不再追加 |

如果文档之间出现冲突，采用以下优先级：

1. 本文件中的医疗安全、隐私与数据保护底线。
2. 用户在当前对话中不违反上述底线的最新明确要求。
3. `docs/development-requirements.md` 与 `docs/quality-security-standards.md`。
4. `docs/technical-design-spec.md` 与 `docs/execution-steps.md`。
5. `docs/implementation-plan-v1.md`。
6. `docs/project-plan-v1.md`。
7. 较早的开发日志和其他说明。

发现冲突时不能静默选择。先在当天开发日志记录冲突、选择和影响，再同步修正文档。

## 3. 已冻结的第一版范围

第一版只开发 iPhone App。

必须完成的核心闭环：

```text
HealthKit 数据读取
    → 数据质量判断
    → 个人基线与趋势
    → 主观感受和生活情境
    → 可解释洞察
    → CareKit 微计划
    → 执行结果评估
    → 我的有效方法
    → 基于健康事实包的 AI 对话
```

第一版明确不做：

- iPad、Mac 和网页端。
- 独立 watchOS App。
- Yaoyao 跳绳计数整合。
- Android 和其他穿戴品牌。
- 医院挂号、实时号源、支付和真人在线问诊。
- 疾病诊断、处方和药物剂量调整。
- 完整 FHIR、CareKitFHIR 或医院信息系统。
- 社区、排行榜和好友竞争。
- 大型端侧语言模型。
- 复杂会员、广告和商业化系统。

用户没有明确扩展范围时，不得因为“以后可能有用”而添加上述功能。新增需求先记录为未来候选，不得挤占 P0 闭环。

## 4. 产品不可破坏的差异化

下列能力是知衡的原创核心，不能被通用聊天、单一健康评分或开源项目原样替代：

1. 个人基线优先：主要比较用户和过去的自己。
2. 数据质量守门员：数据不足、断档或未佩戴时不输出强结论。
3. 主客观对照：客观数据和用户感受具有同等地位。
4. 生活情境：通过少量标签或最多一个关键追问补足背景。
5. 事实、解释、建议分层：相关性不得写成因果性。
6. 微计划：一次只建议一个 3～7 天、低风险、可停止的行动。
7. 效果验证：同时比较客观指标、主观感受、完成率和数据质量。
8. 我的有效方法：多次积累适合用户自己的方法，而不是一次性建议。
9. 低焦虑表达：不使用红色恐吓、每日综合健康分或确定疾病措辞。
10. AI 可追溯：回答必须能显示使用了哪些事实和时间范围。

## 5. HealthGPT 与 CareKit 的分工

### HealthGPT

HealthGPT 是健康数据进入 AI 对话方向的重点参考，主要研究：

- HealthKit 权限和数据获取模式。
- 健康数据如何聚合成模型上下文。
- 对话状态、模型接口、失败与重试。
- 远程和本地模型的抽象方式。
- 开源结构、测试和许可证处理。

不得直接换名称、换界面后参赛。不得照搬品牌、完整交互、提示词或不符合知衡隐私和安全原则的数据上传逻辑。

任何依法复用的代码必须记录来源、许可证、版本或提交，并加入开源软件声明。个人基线、趋势、主客观对照、计划效果和“我的有效方法”必须保持为知衡自己的实现。

### CareKit

CareKit 是第一版确定重点使用的核心框架，负责洞察之后的行动闭环：

| 知衡概念 | CareKit 对应 |
| --- | --- |
| 一次 3～7 天微计划 | `OCKCarePlan` |
| 每日行动 | `OCKTask` |
| 日期、时间和目标 | `OCKSchedule` |
| 某次执行记录 | `OCKOutcome` |
| 完成量、困难度或反馈 | `OCKOutcomeValue` |
| 完成率 | `CareTaskProgressStrategy`、`OCKAdherenceQuery` 或基于 Outcomes 的等价计算 |

强制数据职责：

- CareKitStore 是微计划、任务、日程和执行结果的唯一事实来源。
- SwiftData 不得重复保存同一份任务完成状态。
- SwiftData 保存每日主观记录、生活事件、洞察、分析快照和“我的有效方法”。
- 两类存储使用稳定字符串 ID 关联，如 `carePlanID`、`taskID` 和 `insightID`。
- HealthKit 保存和提供客观健康数据；知衡只读取用户授权的数据。
- 知衡算法负责计划效果分析；CareKit 负责可靠记录，不负责判断方法是否有效。

CareKitUI 是否直接用于正式页面，需要在 S02 比较后决定。若现成 UIKit 组件妨碍 SwiftUI 体验，可以重点使用 CareKitStore，通过适配层和自定义 SwiftUI 页面呈现。不得因此放弃 CareKit 的核心数据模型。

第一版不使用 CareKitFHIR。

## 6. 默认技术架构

除非完成实施方案中要求的技术验证并记录证据，否则遵循以下默认选择：

- 平台：iPhone。
- UI：SwiftUI。
- 健康数据：苹果原生 HealthKit；SpeziHealthKit 仅作为设计参考，不是第一版依赖。
- 行动计划：CareKit、CareKitStore。
- 其他本地数据：SwiftData。
- 图表：Swift Charts。
- 通知：UserNotifications。
- 网络：URLSession 或经过封装的网络层。
- 敏感配置：Keychain 与不进入版本管理的配置文件。
- 本地分析：纯 Swift、确定性、可单元测试的算法。
- AI：可替换 `AIService` 协议，经结构化健康事实包调用。
- 报告：在 MVP 后段使用 SwiftUI 渲染或 PDFKit。

建议保持以下模块边界：

```text
App / Features
Domain / Protocols / UseCases
Data / HealthKit / CareKit / Persistence / Demo
Analytics / DataQuality / Baseline / Trends / StateComparison / MethodEvaluation
AI / FactPack / Prompts / Safety / Networking
DesignSystem / Support / Resources
Tests / Fixtures
```

页面不能直接散落访问 HealthKit、`OCKStore` 或 AI API。通过协议和服务适配层访问，确保真实数据、模拟数据和测试数据可替换。

## 7. 数据和算法规则

### 数据质量先于趋势

任何趋势、通知和 AI 解释之前先计算数据质量。最低默认规则：

- 最近 7 天至少 4 个有效日，才形成短期趋势。
- 28 天基线至少 14 个有效日，才称为个人基线。
- 缺失数据不能按 0 处理。
- 未佩戴、授权缺失、换设备或数据源变化不能自动解释为健康恶化。
- 所有门槛集中配置并有测试；根据真实数据调整时必须记录原因。

### 趋势必须确定和可追溯

- 优先使用中位数、MAD 等稳健统计，降低单日异常值影响。
- 第一版只输出“未见明确变化”“值得继续观察”“存在持续变化”。
- 每条趋势保留基线、当前值、变化幅度、样本天数、覆盖率和时间范围。
- 固定输入必须产生固定输出。
- AI 不得替代本地程序计算趋势。

### 主客观状态同等重要

每日默认记录精力、压力和身体感受，各为 1～5。客观数据较好但用户感觉较差时，尊重用户感受并询问生活背景或症状；不得用手表数据否定用户。

### 微计划评估禁止伪因果

结果只能表达为：

- 可能有帮助。
- 暂未观察到明显变化。
- 执行不足，无法判断。
- 数据不足，建议继续观察。
- 主观和客观结果不同步。

禁止使用“证明有效”“一定改善”“治疗了”等措辞。单次计划默认只能进入“初步观察”，不能标记成高可信方法。

## 8. AI 和医疗安全

AI 只能解释程序构建的结构化健康事实包，不得直接读取并自由推断全部原始 HealthKit 样本。

事实包只包含完成回答所需的聚合信息：

- 时间范围。
- 数据质量。
- 基线与当前值。
- 程序计算的趋势。
- 用户主动提供的主观感受和生活情境。
- 允许推荐的低风险计划模板。

AI 输出应结构化并经客户端校验，至少区分：事实、可能因素、不确定性、最多一个追问、建议行动和安全级别。格式错误、超时或无网络时降级为本地规则摘要。

禁止行为：

- 确定疾病诊断。
- 处方、停药或剂量调整。
- 把单项指标直接等同于压力或疾病。
- 编造事实包中不存在的数值、事件或症状。
- 隐藏数据不足。
- 承诺持续医疗监护或急救检测。

症状对话只提供一般健康教育、信息整理和就医引导。胸痛、严重呼吸困难、意识异常、严重出血、疑似卒中表现和自伤风险等紧急信号必须由独立安全规则优先处理，提示用户立即寻求当地急救或专业帮助。相关文案发布前需要医学专业人士审阅。

## 9. 隐私和安全

- 默认本地优先处理。
- 只申请当前功能必要的 HealthKit 只读权限。
- 原始 HealthKit 样本默认不上传。
- AI 只接收最小化聚合事实包，并在产品中说明使用的数据。
- 不使用健康数据做广告、出售画像或训练基础模型。
- 不在源代码、测试、开发日志、终端输出、崩溃日志或截图中放入用户真实身份和原始健康数据。
- 模拟数据必须明确标注，不得冒充真实结果。
- API Key 不得写入代码、文档或版本管理；正式分发使用后端代理。
- 用户必须能够查看权限状态并删除主观记录、生活事件、计划相关数据和 AI 对话。
- 锁屏通知默认不显示敏感健康细节。

发现密钥或敏感健康数据可能泄露时，立即停止相关工作，避免继续传播，并把处置记录到不包含秘密本身的开发日志中。

## 10. 工程实现规则

- 一次只推进实施方案中的一个可独立验收任务编号，除非多个小任务天然属于同一原子改动。
- 先实现最小闭环，再增加视觉和高级功能。
- 不为了“看起来完整”加入占位式医疗能力。
- 不维护两套承担相同职责的数据层。
- 不让页面直接耦合第三方框架类型；在 Data/Domain 边界完成映射。
- 新增依赖前记录用途、许可证、固定版本、替代方案和移除成本。
- 优先使用 Apple 原生框架和已经确定的 CareKit。
- 保持真实 HealthKit Provider、Demo Provider 和测试 Fixture 使用同一协议。
- 数据缺失、权限拒绝、网络失败和 AI 失败必须有明确界面状态，不得崩溃或伪造结果。
- 用户已有修改必须保留；修改前检查当前文件状态，不覆盖无关工作。
- 代码和文档中的命名以清楚表达职责为首要目标，避免含糊缩写。

如果实施方案与真实框架 API 不符，先查阅官方文档或仓库并做最小技术验证；不要为了匹配旧文档而编造 API。验证后同步更新实施方案和日志。

## 11. 测试与完成定义

任务只有同时满足以下条件才算完成：

1. 需求在当前范围内。
2. 实现覆盖正常、无数据、错误和权限边界。
3. 完成与风险相匹配的单元测试、集成测试或人工验证。
4. 测试结果已记录，失败没有被隐瞒。
5. 实施方案中的任务状态已更新。
6. 当天 `开发日志/YYYY-MM-DD.md` 已追加记录。
7. 没有提交密钥、真实健康数据或许可证不明代码。

核心算法至少测试：稳定、持续上升、持续下降、单日异常、数据不足和断档。CareKit 至少测试计划创建、Schedule、Outcome、完成率、跨天、提前结束和历史一致性。AI 至少测试数据不足、诊断请求、药物请求、紧急症状、提示注入、错误结构和断网降级。

不能运行某项测试时，日志中明确写出原因、已完成的替代验证和剩余风险，不能写成“已通过”。

## 12. 强制每日开发日志流程

新开发日志位于 `开发日志/`，使用 Asia/Shanghai 日期，每个工作日一个文件：`开发日志/YYYY-MM-DD.md`。制度建立前的 `docs/development-log.md` 作为历史归档保留，不再作为新记录的主要入口。

当天第一次进行实质性工作时，代理必须自动检查并创建当日日志；同一天继续追加。没有开发工作的日期不创建空文件。所谓“自动记录”是开发工作流中的强制动作，不依赖一天结束后回忆补写。

每次开发、设计、测试、依赖变更、架构决策、算法阈值调整、安全问题处理或实质性文档修改后，必须：

1. 在实施方案中找到或创建对应任务编号。
2. 开始时可将任务从 `[ ]` 改为 `[~]`。
3. 完成实现并执行验证。
4. 验证通过后改为 `[x]`；被阻塞时标记 `[!]`。
5. 每完成一项独立工作，立即追加到当天日志“已完成事项”。
6. 同步更新当天日志的待办、问题与风险和唯一下一步。
7. 日志写清任务编号、完成内容、修改文件、验证结果和关键决定。
8. 最终回复用户时概括完成内容、验证结果、当天日志位置和下一步。

没有验证和当日日志的任务不得标为 `[x]`。如果本轮只是回答问题且没有产生项目决定或文件变更，可以不写日志；只要改变了项目事实，就必须记录。日志不得包含 API Key、用户身份、原始健康数据或完整敏感对话。

## 13. 当前执行顺序

除非用户明确改变优先级，按以下顺序推进：

1. 先查看 `docs/execution-steps.md` 的“当前开发批次”，一次只执行一个小任务。
2. S01：确认 macOS、Xcode、iPhone、Apple Watch、开发签名、Apple Health 数据和比赛规则。
3. S02：运行和研究 HealthGPT、CareKit OCKCatalog 与 OCKSample。
4. S02：遵循已完成的比较结论，使用苹果原生 HealthKit 作为唯一健康数据层。
5. S02：使用 CareKitStore 跑通最小三天计划、Schedule、Outcome 和完成率。
6. S03：创建知衡 Xcode 工程与适配层。
7. 后续严格按 `docs/implementation-plan-v1.md` 的 S04～S16 推进。

不要在 S01/S02 证据不足时直接构建大量 UI 或接入正式 AI 服务。

## 14. 交付与沟通

向用户汇报时使用普通人能理解的语言，先说明结果，再说明必要的技术决策。用户不具备开发背景，不要只列类名、API 或编译输出；解释它们解决了什么问题。

每次交付至少说明：

- 已经完成什么。
- 用户现在能看到或验证什么。
- 做了哪些测试，结果如何。
- 有哪些尚未解决的风险。
- 当天开发日志已记录在哪里。
- 下一项具体任务是什么。

不把“创建了文件”描述成“完成了功能”，不把模拟数据描述成真实效果，不把未运行的测试描述成通过。
