# 2026-09-06 开发日志

## 今日目标

- S11-07～S11-08 / REQ-10 与 S13-01 / REQ-12：完成方法展示控制、证据筛选，并建立通知系统权限请求前的用途说明与用户确认门禁。

## 已完成事项

### 开始｜S11-07｜方法展示控制与历史隔离

- 已确认方法卡是从 CareKit 已结束历史即时生成的只读投影，不能通过删除 Care Plan 来实现“删除方法”。本任务将把用户控制建模为独立本地展示偏好，以稳定模板 ID 关联，不复制或修改 CareKit 执行事实。
- 优先实现可恢复的“隐藏方法”：用户二次确认后从默认方法列表隐藏，并提供查看和恢复入口。存储失败不得显示隐藏成功，读取失败不得把全部方法误判为已隐藏。
- 本任务不修改计划历史页现有的单计划删除语义，不改变方法可信度、完成率、客观/主观变化或再次验证规则，也不调用 AI。

### 完成｜S11-07｜方法隐藏、恢复与 CareKit 历史隔离

- 新增独立版本化 `EffectiveMethodVisibilitySchemaV1`、迁移计划及 SwiftData Store。偏好只保存稳定模板 ID 和隐藏时间，不复制完成率、Outcomes、评估、健康样本或自由文本。
- `MicroPlanSession` 在完整生成 CareKit 方法卡后读取隐藏模板集合，将同一只读投影拆成默认列表与“已隐藏的方法”；隐藏或恢复先保存成功再更新页面。偏好读取失败时不显示可能违反既有偏好的卡片，写入失败时保留原列表。
- “我的”页方法卡新增“隐藏此方法”及二次确认，明确只改变默认列表、计划/打卡/评估历史仍保留；全部隐藏时显示独立状态，并可展开“已隐藏的方法”恢复显示。
- 演示模式在 CareKit 与显示偏好读取前短路，也不允许写入真实偏好。隐藏与恢复路径不调用 `CarePlanService.deletePlan`，不改变可信度、再次验证或计划历史删除语义。
- 修改文件：`Zhiheng/Zhiheng/Infrastructure/EffectiveMethodVisibilityStore.swift`、`Zhiheng/Zhiheng/Presentation/Plans/MicroPlanView.swift`、`Zhiheng/Zhiheng/RootTabView.swift`、`Zhiheng/ZhihengTests/EffectiveMethodTests.swift`、Xcode 工程文件及本任务对应标准文档。

### 验证｜S11-07

- 首次专项测试在编译测试代码时发现 3 处把 actor 异步读取直接写进 `XCTAssertEqual` 自动闭包；已改为先 `await` 取值再断言。首次受限沙箱运行还因模拟器与 SwiftPM 缓存权限失败，随后按授权环境重跑。两类问题均已修正或排除，不计入最终通过结果。
- 方法专项：41 项通过，0 失败、0 跳过。新增 6 项覆盖 SwiftData 跨上下文隐藏/恢复、未知模板整组拒绝、隐藏与恢复不删除 CareKit 历史、偏好读失败、写失败保持原状态及演示零读写。
- 完整 iOS 回归：478 项通过，0 失败、0 跳过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_08-36-59-+0800.xcresult`。
- Release 通用真机构建通过：`xcodebuild build -project Zhiheng/Zhiheng.xcodeproj -scheme Zhiheng -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO`。
- `plutil -lint Zhiheng/Zhiheng.xcodeproj/project.pbxproj` 与 `git diff --check` 通过。
- iPhone 17 Pro / iOS 26.5 模拟器实际交互通过：关闭每日感受弹窗后看到“隐藏此方法”；打开二次确认并核对历史保留文案；确认后看到“当前方法均已隐藏”；展开“已隐藏的方法（1）”后看到“计划与评估历史仍保留”和“恢复显示”；恢复后原方法卡重新出现。验证结束时已恢复显示，没有遗留隐藏状态。

### 开始｜S11-08｜方法证据筛选

- 已确认筛选只作用于 S11 方法卡的只读运行时投影，不新增持久化、不修改 CareKit 历史、隐藏偏好、可信度或再次验证规则。
- 为避免把模板用途写成个人效果，四类筛选只使用卡片已有结构化事实：睡眠为至少一项睡眠指标同期方向向好；精力为计划期中位数高于计划前；压力为计划期中位数低于计划前；可坚持性为 CareKit 加权历史完成率至少达到既有 60% 评估门槛。
- 页面需解释筛选依据只是同期变化或历史完成率，不表示计划造成结果，也不保证未来一定容易坚持。缺少相应可判断事实时不匹配，并提供清晰空结果与“查看全部”。

### 完成｜S11-08｜睡眠、精力、压力与可坚持性筛选

- 新增纯 Swift `EffectiveMethodEvidenceFilter`，固定提供“全部、睡眠、精力、压力、可坚持性”五类，并保持来源卡片顺序。
- 睡眠只匹配方向向好的入睡时间或睡眠时长；精力和压力分别只匹配完整主观对照中的有利方向，并用 `0.05` 排除浮点噪声；可坚持性复用既有 60% CareKit 加权完成率门槛。缺失、不可读、零日程或中性证据均不匹配。
- “我的”页新增横向筛选控件、选中状态、当前依据、非因果/非保证说明、无匹配状态和“查看全部”。筛选只作用于未隐藏方法卡，不影响“已隐藏的方法”管理区，也不持久化或改写 CareKit 历史、可信度和再次验证状态。
- 修改文件：`Zhiheng/Zhiheng/Domain/Models/EffectiveMethod.swift`、`Zhiheng/Zhiheng/RootTabView.swift`、`Zhiheng/ZhihengTests/EffectiveMethodTests.swift` 及本任务对应标准文档。

### 验证｜S11-08

- 前两次专项编译分别暴露了布尔函数缺少显式返回、以及带 `guard` 分支不能写成 Swift `switch` 表达式；已统一改为普通 `switch` 的显式返回，问题修正后重新执行全部验证。
- 方法专项：46 项通过，0 失败、0 跳过；新增 5 项覆盖稳定分类与门槛、睡眠指标和方向、精力/压力互不替代、60% 加权完成率边界、顺序与只读性。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_08-49-31-+0800.xcresult`。
- 完整 iOS 回归：483 项通过，0 失败、0 跳过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_08-52-14-+0800.xcresult`。
- Release 通用真机构建通过：`xcodebuild build -project Zhiheng/Zhiheng.xcodeproj -scheme Zhiheng -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO`。
- iPhone 17 Pro / iOS 26.5 模拟器实际交互通过：“可坚持性”保留 100% 完成率方法；“睡眠”在无可判断睡眠对照时显示明确空结果；“查看全部”恢复原方法卡。筛选按钮的辅助功能 Label 与选中 Value 可读取。
- `git diff --check` 通过；工程权限文件 `Zhiheng/Zhiheng/Zhiheng.entitlements` 的 `plutil -lint` 通过。工程未使用独立 `Info.plist` 文件，由 Xcode 构建设置生成，因此没有虚构对应检查结果。

### 开始｜S13-01｜通知授权前用途说明

- 本任务只建立通知权限状态读取、用途说明页和用户确认后的系统授权请求，不实现 S13-02～S13-04 的签到、微计划或趋势通知调度。
- 用途说明固定覆盖每日签到、微计划和低频趋势三类预期提醒；明确锁屏默认不显示健康数值或敏感细节、常规提醒默认每天最多一条、不是急救监护，并可暂不授权或稍后在系统设置中关闭。
- 系统权限弹窗只能由用户在说明页点击明确的继续按钮触发；进入“我的”页、读取权限状态、打开说明页或取消说明均不得请求授权。

### 完成｜S13-01｜通知状态、用途说明与确认门禁

- 新增 `NotificationAuthorizationClient` 与 actor 隔离的 `SystemNotificationAuthorizationClient`，把 `UserNotifications` 状态映射为本地可测试状态；未知状态不按已开启处理。
- 新增主线程 `NotificationAuthorizationSession`：刷新只读状态，打开/取消说明不请求权限，只有说明页仍显示且状态仍未请求时的明确确认才允许调用一次系统授权。拒绝后不反复请求，请求失败保留说明和普通语言错误供主动重试。
- “我的”页新增“提醒”状态区与用途说明页，列出每日感受、微计划和低频趋势三类用途，并固定显示每天最多一条常规提醒、锁屏隐私、关闭方式及非急救监护边界。页面明确当前授权不会读取新健康数据或安排通知。
- 本任务未创建任何 `UNNotificationRequest`、通知偏好 Store 或调度逻辑，S13-02～S13-06 的职责保持独立。
- 修改文件：`Zhiheng/Zhiheng/Presentation/Components/NotificationAuthorization.swift`、`Zhiheng/Zhiheng/RootTabView.swift`、`Zhiheng/ZhihengTests/NotificationAuthorizationTests.swift`、Xcode 工程文件及对应标准文档。

### 验证｜S13-01

- 首次专项编译发现 `withCheckedThrowingContinuation` 无法从 `Void` 回调自动推断泛型，已显式声明 `CheckedContinuation<Void, Error>` 并重跑通过。
- 通知专项 8 项通过，覆盖三类用途、频率/隐私/安全文案、状态映射、刷新零请求、打开/取消零请求、确认单次请求、拒绝不重试及失败保留说明。文案收尾后的最终定向结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_09-12-40-+0800.xcresult`。
- 完整 iOS 回归：491 项通过，0 失败、0 跳过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_09-07-38-+0800.xcresult`。
- Release 通用真机构建通过：`xcodebuild build -project Zhiheng/Zhiheng.xcodeproj -scheme Zhiheng -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO`。
- 文案收尾后尝试再次运行完整回归时，受限环境无法访问 `CoreSimulatorService`、Xcode/SwiftPM 缓存；按规则申请沙箱外重跑又因当前 Codex 使用额度被系统拒绝，因此没有把该次基础设施失败伪装成测试结果，也没有绕过权限限制。收尾改动仅涉及通知状态说明文字；同一最终代码的通知专项 8 项已重跑通过，完整功能版此前的 491 项回归和 Release 构建证据保留有效。
- iPhone 17 Pro / iOS 26.5 模拟器实际验证：进入“我的”无系统权限弹窗；打开说明页能完整读取三类用途与边界；“暂不开启”返回后仍为未请求；点击明确允许按钮后才出现 iPhone 系统授权框；选择“不允许”后页面显示“通知已在系统中关闭”且没有重复请求入口。
- 尝试用 `simctl privacy ... reset notifications` 清理测试权限时，模拟器返回 `Operation not permitted`；没有伪装成功。由于该 App 此前从未请求通知权限，现有状态本身就是 `notDetermined`，因此仍完成了完整首次授权路径。测试结束时该模拟器中的知衡通知权限为拒绝，不影响代码或真机设置。

## 待办事项

- [x] S11-07：实现方法隐藏、恢复、持久化、失败保护、历史隔离测试和页面验证。
- [x] S11-08：实现按睡眠、精力、压力和可坚持性筛选，并完成边界测试、模拟器交互与全量回归。
- [x] S13-01：请求通知权限前说明用途，并完成系统状态、失败边界和模拟器首次授权验证。
- [x] S13-02：实现每日签到提醒。
- [x] S13-03：实现微计划提醒。
- [x] S13-04：实现低频趋势提醒。

## 问题与风险

- “删除方法”若直接映射为删除 CareKit 历史，会破坏执行事实和可信度来源；本轮采用可恢复的隐藏偏好满足用户控制，并在界面明确历史仍保留。
- S11-06 确认弹窗原锁屏限制已在本轮模拟器实际交互环境恢复后解除；S11-07 的隐藏确认、隐藏和恢复均已点击验证。
- 可恢复隐藏按模板作用：同一模板后续新形成的方法卡仍沿用该偏好，直到用户主动恢复。这与方法候选按模板归并的领域身份一致。
- 当前筛选依据是最近一次可展示的完整对照或累计完成率，只帮助整理已有观察，不代表计划导致变化，也不预测下一次执行结果；页面已明确该限制。
- 每日提醒使用 30 天滚动一次性请求；用户正常使用或保存签到时会续排。若连续超过 30 天不再打开知衡，滚动窗口自然停止，不会变成无限后台监护。
- 模拟器当前通知权限为拒绝，无法通过 `simctl privacy` 修改；真实通知调度与送达仍需用户在自己的授权 iPhone 或可修改权限的隔离模拟器复验。

## 下一步唯一任务

- S13-04：实现低频趋势提醒；继续复用每日总通知预算和 S09-07 同类提醒偏好。

### 开始｜S13-04｜低频趋势提醒与同类去重

- 本任务只增加默认关闭的低频趋势提醒，不提前实现 S13-05 的通用频率和安静时间设置，也不处理用户已明确无需继续调优的大字体。
- 只复用本地 `InsightFactGenerator` 的 7/28 天聚合事实和既有趋势门槛；只有数据质量合格、来源稳定、非单日异常且达到“持续变化”才形成候选，AI 不参与趋势计算或通知选择。
- 同一“指标主题＋变化方向”使用不可逆散列身份只处理一次，并读取 S09-07 的“关闭同类提醒”偏好。任一健康事实、主题偏好或系统调度读取失败都不通知。
- 三类常规提醒的同日优先级固定为微计划、低频趋势、每日感受，合计每天最多一条；趋势锁屏文案保持中性，不显示指标名、方向、数值、症状或异常措辞。

### 开始｜S13-02｜每日签到提醒

- 本任务只实现每日感受提醒的本地偏好、时间选择、调度和取消，不提前加入微计划提醒、趋势提醒或通用安静时间。
- 默认关闭；只有用户主动开启、系统通知权限允许、处于真实数据模式且本地签到记录可可靠读取时才安排。当天已有记录时从次日开始，保存或删除今日记录后重新计算。
- 采用带稳定日期标识的有限滚动一次性通知，避免永久重复触发器无法在用户当天签到后抑制同日提醒；锁屏正文仅使用中性提示，不包含评分、症状、计划反馈或健康数值。

### 完成｜S13-02｜每日感受提醒

- 新增本地确定的每日提醒策略、`UserNotifications` 调度适配器与主线程会话；偏好默认关闭，仅保存开关和时分，默认时间为 20:00。
- 未签到且时间未到从当天开始；已签到或时间已过从次日开始。未来 30 个本地公历日各使用一个稳定标识，更新时只替换每日感受前缀的请求，不误删其他通知；部分写入失败会回收本次已添加项。
- Root 在启动、进入前台、模式变化和权限刷新后同步；今日签到保存或历史修改/删除成功后发出重算信号。关闭、撤权、演示模式与签到读取失败统一清理待发送项，失败不显示虚假成功。
- “我的”页新增“每日感受提醒”开关和时间选择；系统未允许且当前未开启时不可误开，仍可关闭已有偏好。页面固定说明锁屏不含感受评分、症状或健康数值，并保证该类型同日最多一条。
- 通知可见内容固定为“留一点时间给今天 / 打开知衡，记录今天的感受。”，不读取 HealthKit、CareKit、AI、备注或其他自由文本。
- 修改文件：`DailyCheckInReminder.swift`、`NotificationAuthorization.swift`、`SubjectiveCheckInSession.swift`、`RootTabView.swift`、`DailyCheckInReminderTests.swift`、Xcode 工程文件及对应标准文档。

### 验证｜S13-02

- 首次编译发现系统待发送请求对象不能安全跨 Swift 6 actor 边界，已改为在系统回调内只提取可发送的字符串标识后再跨边界；未降低并发检查。
- 每日提醒专项 12 项与原授权专项 8 项通过，覆盖默认关闭、四重门禁、当天/次日边界、日期唯一、锁屏中性内容、偏好恢复、时间更新、签到保存/历史变更联动、读取失败、撤权、关闭和调度失败。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_11-57-04-+0800.xcresult`。
- 第一次完整回归使用 `CODE_SIGNING_ALLOWED=NO`，503 项中只有既有 Keychain 测试因测试宿主缺少 entitlement（`-34018`）失败；单独复跑仍得到同一环境错误，没有伪装通过。改用模拟器正常本地签名后，该 Keychain 测试通过，最终完整回归 503 项通过、0 失败、0 跳过。最终结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_12-01-04-+0800.xcresult`。
- Release 通用真机构建通过：`xcodebuild build -project Zhiheng/Zhiheng.xcodeproj -scheme Zhiheng -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO`。
- iPhone 17 Pro / iOS 26.5 模拟器实际检查：通知权限拒绝状态清晰；每日感受开关为关闭且不可误启用；关闭提示、锁屏中性内容和每天最多一条均可见。尝试用 `simctl privacy` 授权返回 `Operation not permitted`，没有代替用户修改系统设置，也没有把开启态或真实送达冒充为已验证；模拟器字体已恢复标准大小。

### 开始｜S13-03｜微计划提醒与每日总预算

- 本任务只增加默认关闭的微计划提醒，不提前实现低频趋势、通用频率或安静时间；AI 不参与通知选择。
- 提醒事实只从同一个 `CarePlanService` 读取 CareKit 当前计划、Schedule 与 Outcomes，不复制到 SwiftData，也不读取反馈文本。
- 微计划在同日冲突时优先于每日感受，但两类常规提醒最终每天合计最多一条；锁屏不显示计划标题、具体行动、反馈、完成率或健康详情。

### 完成｜S13-03｜CareKit 守门、计划联动与中性通知

- 新增 `MicroPlanReminderPolicy`、系统调度协议和主线程会话。“我的”页新增默认关闭的“微计划提醒”开关；开启后按计划内既有时间显示下一次提醒，不新增另一份时间配置。
- 只有系统允许、真实模式、CareKit 当前计划为活动状态、对应 Schedule occurrence 尚无 Outcome 且时间仍在未来时才排程。完成或跳过、暂停、结束、无活动计划、撤权、演示模式、CareKit 读取失败与调度失败均安全清理或不显示虚假成功。
- Root 与计划会话共享同一个 CareKit 适配器；启动、前台、模式/权限变化，以及活动计划或今日 Outcome 变化后重算。未向 SwiftData 写入任务、Schedule 或完成状态，未读取反馈，未调用 AI。
- `SystemNotificationReminderScheduler` 统一管理两个前缀：微计划成功写入后移除同日本地感受请求；每日感受重排也会跳过已有微计划日期，其他日期保留。锁屏固定为“给今天留一个小步骤 / 打开知衡，查看今天的计划。”。
- 修改文件：`MicroPlanReminder.swift`、`DailyCheckInReminder.swift`、`NotificationAuthorization.swift`、`RootTabView.swift`、`MicroPlanReminderTests.swift`、`DailyCheckInReminderTests.swift`、Xcode 工程文件及本任务对应标准文档。

### 验证｜S13-03

- 第一次通知专项编译发现每日预算集合的 `compactMap` 返回类型无法推断，已显式标注 `String?` 并重跑通过；没有隐藏该失败。
- 新增微计划提醒测试 10 项通过，包含默认关闭零 CareKit 读取、完整门禁、稳定日期标识、已记录/已过时间跳过、暂停、演示、读取与调度失败、锁屏文案、每日预算及真实内存 CareKitStore 的 Schedule/Outcome 集成路径。
- 通知相关专项共 30 项通过；完整 iOS 回归 513 项通过、0 失败、0 跳过。最终结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_12-24-59-+0800.xcresult`。
- Release 通用 iPhone 无签名构建成功；`plutil -lint Zhiheng/Zhiheng.xcodeproj/project.pbxproj` 与 `git diff --check` 通过。
- 本轮尝试用界面检查工具连接正在运行的 Simulator，两次返回 `timeoutReached (-10005)`；因此没有把新增开关的模拟器实际开启态写成已验证。模拟器通知权限仍为拒绝，真实排程与送达继续保留为授权真机或可修改权限的隔离环境复验；代码路径已由策略、会话、共享预算和真实内存 CareKitStore 测试覆盖。

### 完成｜S13-04｜质量守门、同类去重与三类通知预算

- 新增纯本地低频趋势候选规则，只复用共享健康快照经 `InsightFactGenerator` 形成的 7/28 天聚合事实。只有短期/基线有效日、来源单一稳定、单日异常值保护和“存在持续变化”全部通过时才按幅度与实际门槛选择最多一个候选；“值得继续观察”、无明确变化和所有质量降级均不通知。
- “我的”页新增默认关闭的“低频趋势提醒”开关。开启、权限、真实模式、健康事实和 S09-07 同类主题偏好缺一不可；洞悉页关闭或恢复同类提醒后会立即触发重算，偏好读取失败不冒充允许。
- 同一“指标主题＋高/低方向”使用不含指标数值的 SHA-256 散列记录为已处理，分析窗口逐日移动不会重复创建；尚未到时的同一请求保留原触发日期，不会因每次前台刷新而向后漂移。
- 共享系统调度器扩展为三类前缀，同日优先级固定为微计划、低频趋势、每日感受。微计划移除同日趋势和签到，趋势跳过微计划并移除同日签到，签到避开两者；调用顺序变化仍每天最多一条。
- 趋势锁屏固定使用“有一条新的个人观察 / 打开知衡，查看近期趋势。”，不包含指标、方向、数值、症状、异常或 AI 内容。AI 未参与计算、选择或调度。
- 修改文件：`LowFrequencyTrendReminder.swift`、`DailyCheckInReminder.swift`、`RootTabView.swift`、`InsightsView.swift`、`LowFrequencyTrendReminderTests.swift`、Xcode 工程文件及本任务对应标准文档。

### 验证｜S13-04

- 第一次受限沙箱运行因 CoreSimulator 与 SwiftPM 缓存权限失败，按规则切换到授权环境；第一次授权环境编译发现测试授权 actor 缺少显式初始化器，补齐后重新执行。两次失败均已如实保留，没有计为通过。
- 新增低频趋势专项 10 项通过，覆盖持续变化与质量门槛、高低方向散列、完整开关门禁、下一日间时点、同趋势去重与待发送日期稳定、三类优先级、锁屏中性文案、默认关闭零读取、偏好持久化、主题关闭/读失败、演示和调度失败。
- 通知专项共 40 项通过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_12-44-00-+0800.xcresult`。
- 完整 iOS 回归 523 项通过、0 失败、0 跳过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_12-45-12-+0800.xcresult`。
- Release 通用 iPhone 无签名构建成功；`plutil -lint Zhiheng/Zhiheng.xcodeproj/project.pbxproj` 与实现前的 `git diff --check` 通过，文档收口后将再次执行最终差异检查。
- 尝试读取已启动模拟器以复验“我的”页时，运行环境因当前使用额度门禁拒绝执行，未绕过限制。因此没有把 S13-04 新增开关的模拟器实际页面或真实通知送达写成已验证；两者保留为用户授权真机或可修改权限的隔离环境复验。用户已明确无需继续处理大字体，本任务未新增相关改动。

## 当前待办（S13-04 收口后）

- [x] S13-04：实现低频趋势提醒、同类去重、三类每日预算与失败保护。
- [ ] S13-05：提供安静时间、频率和关闭设置。

## 当前问题与风险（S13-04 收口后）

- 通知候选、去重、偏好联动和预算已由自动测试验证；由于当前模拟器通知权限为拒绝且本轮界面检查又被运行环境额度门禁阻止，实际开启态与锁屏送达仍需在授权真机复验。
- 已处理散列仅表达规则版本、指标主题和方向，不含健康值；这是有意的保守去重。后续 S13-05 调整频率时不得清除该历史并造成同一趋势再次轰炸。

## 下一步唯一任务（S13-04 收口后）

- S13-05：提供安静时间、频率和关闭设置；统一三类提醒的时间窗口和频率控制，不改变事实来源、优先级与医疗安全边界。

### 开始｜S13-05｜统一安静时间、频率与全部关闭

- 本任务只在三类既有提醒之上增加统一投递设置，不提前实现 S13-06 的锁屏文案复核，也不调整用户已明确无需继续处理的大字体。
- “全部关闭”只清除三类待发送通知并保留各类型开关、时间选择和低频趋势已处理散列，重新开启后按原偏好恢复计算，避免造成同一趋势重复提醒。
- 安静时间内的候选直接跳过，不把微计划提醒擅自提前或延后到脱离 CareKit Schedule 的时间；频率作为三类常规提醒共享的最小自然日间隔，继续保持“微计划 > 低频趋势 > 每日感受”的优先级。

### 完成｜S13-05｜共享投递门禁与用户控制

- 新增版本化统一投递设置：总开关默认开启，三类具体提醒仍分别默认关闭；安静时间默认本地 22:00～08:00，可关闭或修改起止；频率提供每天最多一条、至少间隔 2 天及至少间隔 7 天。
- 新增纯本地确定性投递策略，先跳过安静时段候选，再按稳定标识中的本地公历日执行共享最小间隔。高优先级请求会阻止或回收间隔窗口内的低优先级请求，顺序保持微计划、低频趋势、每日感受。
- “全部提醒”关闭时三类同步均先清理自己的系统请求且不再写入；每日感受时间、三个类型开关和低频趋势已处理散列均保留，重新开启不会把同一趋势当成新候选重复提醒。
- 安静时段内直接跳过原候选，不提前或延后 CareKit Schedule。三个会话均以系统调度器返回的实际请求更新下一次时间；微计划和趋势被过滤时显示安静时间、频率或优先级原因，不冒充已安排。
- “我的”页已接入总开关、频率选择、安静时间开关及两个时间选择器；关闭总开关后明确说明待发送请求已清理、偏好和趋势去重仍保留。
- 修改文件：`NotificationDeliverySettings.swift`、`DailyCheckInReminder.swift`、`MicroPlanReminder.swift`、`LowFrequencyTrendReminder.swift`、`RootTabView.swift`、`NotificationDeliverySettingsTests.swift`、`MicroPlanReminderTests.swift`、Xcode 工程文件及本任务对应标准文档。

### 验证｜S13-05

- 首次受限沙箱构建因 CoreSimulator、SwiftPM 与 Clang 缓存权限失败，按规则切换到授权环境；首次授权编译发现把同一个 `UserDefaults` 实例跨主线程与 actor 发送存在 Swift 6 数据竞争风险，改为带锁的共享设置 Store 后 Debug 模拟器构建通过。失败均未计为通过。
- 新增统一设置与策略测试 12 项，并补充 1 项微计划会话使用调度后实际结果的测试。通知专项共 53 项通过、0 失败、0 跳过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_17-44-06-+0800.xcresult`。
- 完整 iOS 回归 536 项通过、0 失败、0 跳过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_17-45-27-+0800.xcresult`。
- Release 通用 iPhone 无签名构建成功；文档收口后的 `plutil -lint Zhiheng/Zhiheng.xcodeproj/project.pbxproj` 与 `git diff --check` 最终检查均通过。
- iPhone 17 Pro / iOS 26.5 隔离模拟器实际检查通过：标准字号下总开关、三档频率、安静时间开关、22:00/08:00 时间选择及三类提醒均完整显示；关闭总开关后频率与时间控件收起，保留说明可见，三类开关保持关闭且无布局遮挡。按用户要求未继续处理大字体。

## 当前待办（S13-05 收口后）

- [x] S13-05：提供统一安静时间、频率和全部关闭设置。
- [ ] S13-06：锁屏文案默认不暴露敏感健康细节。

## 当前问题与风险（S13-05 收口后）

- 设置默认值、持久化、过滤、优先级、实际调度结果和界面开启/关闭布局均已验证；当前模拟器通知系统权限仍未开启，因此真实锁屏送达、跨时区后系统现存请求的实际刷新与用户真机触感仍需在授权 iPhone 复验。
- “安静时间内直接跳过”是为避免移动 CareKit 计划时间的保守选择，页面已明确说明。S13-06 只复核和收口锁屏可见内容，不应放宽该策略或引入健康敏感字段。

## 下一步唯一任务（S13-05 收口后）

- S13-06：锁屏文案默认不暴露敏感健康细节；复核三类通知的锁屏内容、系统预览边界与自动化门禁，不提前实现 7 天健康摘要。

### 开始｜S13-06｜锁屏公开内容隐私收口

- 审计发现现有三类固定文案虽不包含评分、指标值、症状或自由文本，但仍分别暴露“感受、计划、趋势”的提醒类型；本任务将三类锁屏公开内容统一为不透露类别的中性提示。
- 系统调度器改为只接收内部提醒类型并从集中隐私策略生成公开内容，不再允许各业务策略把任意标题或正文传入 `UNNotificationRequest`；通知负载不得携带健康事实、主观记录、CareKit 详情、AI 文本、附件或自定义操作。
- 本任务只完成 S13-06 的锁屏内容边界与自动化验证，不提前实现 S13-07 的 7 天健康摘要，也不继续处理用户已明确无需调整的大字体。

### 完成｜S13-06｜三类统一公开预览与空敏感负载

- 新增集中锁屏隐私策略，将每日感受、微计划和低频趋势三类内部提醒全部映射为同一公开标题“知衡提醒”和正文“打开知衡查看。”；锁屏不再透露提醒类别。
- 系统调度器改为只接收内部提醒类型，三个业务策略不再持有或传入通知标题、正文，后续新增通知也必须经同一策略创建系统内容。
- `UNMutableNotificationContent` 不携带 `userInfo`、附件、类别、线程、目标内容、角标或启动图片；没有复制健康指标、数值、症状、签到、计划、反馈、趋势、来源、AI 输出或自由文本。
- “我的”页和通知授权前说明同步展示实际固定预览，并说明不会透露感受、计划或趋势类别。系统是否显示预览继续由用户的 iPhone 设置控制。
- 修改文件：`ReminderLockScreenPrivacy.swift`、`DailyCheckInReminder.swift`、`MicroPlanReminder.swift`、`LowFrequencyTrendReminder.swift`、`NotificationAuthorization.swift`、`RootTabView.swift`、四个通知测试文件、Xcode 工程文件及对应标准文档。

### 验证｜S13-06

- 第一次通知专项运行中，新增 6 项锁屏测试已经通过，但既有授权说明测试仍断言旧的连续文本；同时最初尝试校验两个在 iOS 15 起被系统忽略的摘要字段，编译产生弃用警告。已更新说明断言并移除无效字段及对应测试，没有把该轮记为通过。失败结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_18-01-24-+0800.xcresult`。
- 修复后通知专项 59 项通过、0 失败、0 跳过，覆盖三类完全同文、敏感词与数字禁入、真实系统内容的公开字段和负载为空、授权说明与实际预览一致。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_18-02-44-+0800.xcresult`。
- 完整 iOS 回归 542 项通过、0 失败、0 跳过。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_18-03-34-+0800.xcresult`。
- Release 通用 iPhone 无签名构建成功；`plutil -lint Zhiheng/Zhiheng.xcodeproj/project.pbxproj` 与 `git diff --check` 通过。源码审计只在集中隐私策略中发现 `UNMutableNotificationContent` 创建点，旧三类文案和业务标题/正文入口均已移除。
- iPhone 17 Pro / iOS 26.5 模拟器实际检查通知授权说明页，精确固定预览和不显示提醒类型、健康数值、症状、计划反馈的边界均完整可见。没有替用户修改系统通知权限；当前模拟器未授权，因此真实锁屏送达继续保留授权真机复验。按用户要求未继续处理大字体。

## 当前待办（S13-06 收口后）

- [x] S13-06：锁屏文案默认不暴露敏感健康细节。
- [ ] S13-07：生成可预览的 7 天健康摘要。

## 当前问题与风险（S13-06 收口后）

- 公开内容、系统负载和授权说明均已由自动化及模拟器页面验证；当前模拟器通知权限未开启，实际锁屏样式与送达触感仍需用户在授权 iPhone 上复验。
- iPhone 的系统级通知预览设置由用户控制。即使用户选择始终显示预览，知衡当前也只提供统一中性文案；应用不会也不能替用户更改系统设置。

## 下一步唯一任务（S13-06 收口后）

- S13-07：生成可预览的 7 天健康摘要；先定义摘要事实范围、数据质量门禁和主客观信息分层，不提前实现导出分享。

### 开始｜S13-07｜本地 7 天健康摘要预览

- 本任务固定使用截至今天零点的最近 7 个完整本地日，客观部分仍与此前不重叠的 28 天个人基线比较；复用既有趋势、来源与质量门槛，不重新实现算法，也不把尚未结束的今天混入。
- 摘要固定展示睡眠时长、HRV、静息心率和步数四项客观事实；主观部分只聚合三项结构化评分和有记录天数，生活事件只按类型计数，不复制备注、自定义名称或记录 ID。
- 建议复用既有四层洞察本地规则并显示来源，明确未使用 AI、不会自动创建计划。演示模式不读取真实感受，主观读取失败不展示部分结果。
- 本轮只新增“我的”页 App 内预览和自动化门禁，不落库、不新增 HealthKit 查询，不提前实现 S13-08 的 PDF、系统分享或导出隐私确认；按用户要求不处理大字体。

### 完成｜S13-07｜可追溯的 7 天健康摘要预览

- 新增独立的内存摘要投影与工厂，复用 `InsightFactGenerator`、`InsightContextLoader` 和 `InsightFourLayerCardFactory`。摘要范围固定为截至今天零点的最近 7 个完整本地日，客观记录对照此前不重叠的 28 天个人基线，不新增 HealthKit 查询、不保存摘要、不调用网络或 AI。
- 客观部分固定展示睡眠时长、HRV、静息心率和步数，保留近 7 天中位数、7/28 天有效日、相对变化、趋势状态、可读来源和明确不可用原因；缺失、未授权和质量不足不会补 0 或伪装为健康结论。
- 主观部分分别汇总精力、压力、身体感受的 1～5 分中位数及记录天数，不计算综合健康分；生活情境只按结构化事件类型计数，不复制备注、自定义名称或记录 ID，也不把同期事件写成原因。
- 下一步建议复用既有四层洞察本地规则，页面明确展示来源、未使用 AI、不会自动创建微计划。演示模式不读取真实主观记录；主观读取失败时客观事实仍可展示，但不拼接部分主观内容。
- “我的”页新增“健康摘要”入口和可刷新预览页，展示摘要范围、本周概览、客观记录、主观感受、生活情境、下一步及非诊断/非急救边界。本步没有提前加入 PDF、系统分享或导出确认。
- 修改文件：`SevenDayHealthSummary.swift`、`SevenDayHealthSummaryView.swift`、`RootTabView.swift`、`SevenDayHealthSummaryTests.swift`、Xcode 工程文件、`AGENTS.md` 及本任务对应标准文档。

### 验证｜S13-07

- 首次专项测试构建发现测试夹具创建 `HealthMetricSample` 时漏传必需的 `id`；补入独立 UUID 后重新执行，11 项摘要专项测试全部通过。该失败是测试代码编译问题，没有计为通过。
- 新增测试覆盖 7 个完整日与此前 28 日边界、四项指标顺序与中位数/覆盖率/来源、当前窗口不足、指标不可用、三项主观中位数、未记录、事件类型计数与自由文本剥离、主观读取失败、演示隔离、建议的确定性/无 AI/不自动建计划以及错位窗口拒绝。
- 完整 iOS 回归 553 项通过、0 失败、0 跳过、0 预期失败。结果包：`/Users/chenweiyang/Library/Developer/Xcode/DerivedData/Zhiheng-bpetiiptmhvorucltckikysidyzv/Logs/Test/Test-Zhiheng-2026.09.06_18-22-39-+0800.xcresult`。AI 代理 15 项全部通过。
- Release 通用 iPhone 无签名构建成功；`plutil -lint Zhiheng/Zhiheng.xcodeproj/project.pbxproj`、隐私/依赖静态检索与 `git diff --check` 通过。新摘要代码没有新增 `URLSession`、AI 服务、HealthKit Store、SwiftData 或 CareKit Store 访问点；测试中的私密文本只用于证明不会进入摘要。
- iPhone 17 Pro / iOS 26.5 模拟器实际检查通过。真实模式未连接 Apple Health 时，四项客观记录分别显示“尚未请求访问”和“暂无可用来源”，没有补 0；本机已保存的三项感受和结构化事件仍可独立展示。演示模式明确显示“不是你的真实健康记录”，四项含值事实、有效日、变化和来源完整可见，且明确不读取真实感受、未使用 AI、不会自动创建计划。验收后已恢复真实数据模式。按用户要求未继续处理大字体。

## 当前待办（S13-07 收口后）

- [x] S13-07：生成可预览的 7 天健康摘要。
- [ ] S13-08：支持导出分享版报告并提醒用户注意隐私。

## 当前问题与风险（S13-07 收口后）

- 模拟器已覆盖真实缺失态和演示含值态；用户本人 Apple Health 数据下的来源组合、实际 7/28 天有效日和真机长内容观感仍需在授权 iPhone 上复验，不能由演示数据替代。
- 摘要当前仅在 App 内即时预览，没有生成文件、系统分享或导出记录。任何离开 App 的报告都可能包含敏感健康信息，必须由 S13-08 加入明确隐私确认、最小字段和失败清理后才能开放。

## 下一步唯一任务（S13-07 收口后）

- S13-08：支持导出分享版报告并提醒用户注意隐私；复用同一摘要事实投影生成分享内容，先建立明确确认、敏感范围说明、临时文件生命周期和取消/失败清理，不扩大健康数据或 AI 边界。
