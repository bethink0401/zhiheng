# 2026-09-10 开发日志

## 今日目标

- S14-12：继续修复真实 HealthKit 数据下今日页启动和标签切换卡顿，将步数与活动能量显示正确性纳入同一轮真机验收，并按用户要求移除洞悉页“本周最值得看”卡片。
- S12-19：按用户插入需求，把今日感受、今日变化、生活事件和微计划执行/反馈纳入普通 AI 对话可引用事实，并补齐其他必要的趋势、质量、缺失与隐私字段。

## 已完成事项

### 10:27｜S14-12｜恢复首页原统计口径并确认剩余卡顿

- 完成内容：撤回会改变日期条聚合路径的首次优化，重新安装并打开恢复版；保留同一次 SwiftUI `body` 内只生成一次相同首页展示结果的安全去重。
- 修改文件：`Zhiheng/Zhiheng/TodayView.swift`、`开发日志/2026-09-10.md`。
- 验证结果：`TodayDashboardPresentationTests` 通过；恢复版真机性能采样不再出现此前约 4.93 秒和 3.33 秒的 Severe Hang，但仍出现约 1.22～1.28 秒的主线程 Hang。
- 关键决定：步数与活动能量继续使用原有样本范围、来源选择、累计算法和单位；后续仅将完全相同的展示计算移出主线程并按输入缓存。

### 10:49｜S14-12｜完成同口径后台计算与步数/活动能量回归

- 完成内容：今日页以健康快照时间窗、数据模式、选定日期、日期窗口和五项目标组成稳定请求标识；相同输入只复用一份展示结果，输入变化时继续调用原 `TodayDashboardPresentationFactory`，但将 90 天展示聚合移出主线程。加载期间明确显示“正在整理健康数据”，不伪造 0 值。新增步数与活动能量专项回归，锁定选定日期累计值、单位及 24 小时分布。
- 修改文件：`Zhiheng/Zhiheng/TodayView.swift`、`Zhiheng/ZhihengTests/TodayDashboardPresentationTests.swift`、`开发日志/2026-09-10.md`。
- 验证结果：首页展示与健康数据会话专项 28 项通过；完整 iOS 576 项通过、失败 0、跳过 0；AI 开发代理 15 项通过；Debug 通用真机构建、Release 通用真机构建和 iPhone 安装成功；`git diff --check` 通过。
- 关键决定：首次“日期条只扫五个目标指标”的口径优化保持撤回状态；正式修复不删除所谓“无关样本”，不改变步数、活动能量或其他指标的选样和聚合结果。

### 11:24｜S14-12｜移除洞悉周卡并收敛四个标签页主线程工作

- 完成内容：按用户要求从洞悉页删除“本周最值得看”卡片及其页面加载、交互刷新触发链；洞悉 90 天仪表展示改为稳定请求缓存和后台计算。微计划趋势改为后台计算，AI 助手与微计划对相同数据模式复用已加载的 CareKit 会话状态；“我的有效方法”仍在主线程读取本地 Store 的结构化事实，但趋势、效果判断和卡片构建已移到后台执行。
- 修改文件：`Zhiheng/Zhiheng/InsightsView.swift`、`Zhiheng/Zhiheng/RootTabView.swift`、`Zhiheng/Zhiheng/Presentation/Assistant/AssistantView.swift`、`Zhiheng/Zhiheng/Presentation/Plans/MicroPlanView.swift`、`docs/README.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-10.md`。
- 验证结果：洞悉、CareKit 和有效方法专项回归通过；完整 iOS 576 项通过、失败 0、跳过 0，结果位于 `Test-Zhiheng-2026.09.10_11-20-59-+0800.xcresult`；AI 开发代理 15 项通过；Debug 模拟器、Debug 通用真机和 Release 通用真机构建成功。iPhone 17 Pro 模拟器实际打开洞悉页，确认删除后的页面无该卡片且空数据状态正常。
- 关键决定：本轮仅调整展示计算调度和重复加载，不更改任何健康事实算法；洞察领域模型仍保留给 7 天摘要与既有测试使用，删除的是洞悉页面卡片和触发链。最新真机包构建成功，但连接设备在安装时变为 `unavailable`，不能把本轮标签切换写成真机通过。

### 12:09｜S14-12｜阻止卡片返回时重复加载

- 完成内容：修正 SwiftUI 页面重新出现时 `.task(id:)` 即使输入未变仍再次执行的问题；今日、洞悉和微计划在稳定请求一致时直接保留已显示结果，不再先清空后后台重算；“我的有效方法”以数据模式、快照时间窗和计划进度组成刷新请求，相同输入返回时不再读取 CareKit 历史；设置页的通知状态与提醒同步限定为本次页面首次进入。日期、目标、健康快照、数据模式或计划进度真实变化时仍会生成新请求并刷新。
- 修改文件：`Zhiheng/Zhiheng/TodayView.swift`、`Zhiheng/Zhiheng/InsightsView.swift`、`Zhiheng/Zhiheng/Presentation/Plans/MicroPlanView.swift`、`Zhiheng/Zhiheng/RootTabView.swift`、`docs/README.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-10.md`。
- 验证结果：Debug 模拟器、Debug 通用真机和 Release 通用真机构建成功；完整 iOS 576 项通过、失败 0、跳过 0，结果位于 `Test-Zhiheng-2026.09.10_12-03-47-+0800.xcresult`。iPhone 17 Pro 模拟器使用演示数据分别打开今日睡眠卡和洞悉静息心率卡再返回，原页面节点原地保留，未出现加载状态，步数 3,294 与活动能量 214 千卡保持不变。最新 Debug 包已于 12:09 安装并打开在连接的 iPhone 16 Pro。
- 关键决定：缓存命中只依赖会影响结果的稳定输入，不按“页面出现次数”失效；用户主动下拉刷新或基础输入变化仍正常刷新，避免用永久缓存掩盖新数据。

### 12:24｜S16-11｜准备 GitHub 产品快照

- 完成内容：确认现有远程为 `https://github.com/bethink0401/zhiheng.git`、当前分支为 `main` 且修改前与 `origin/main` 一致；扫描 API Key、私钥、证书、邮箱、手机号和大文件，没有发现真实秘密或身份数据。将 `output/` 与 `竞赛材料/artifacts/` 作为可再生成导出和视觉验收产物加入忽略规则，保留应用源码、测试、产品文档、竞赛材料生成脚本、资源和最终可编辑文档。119 个文件已形成本地产品快照提交 `d71c1d3`，作者使用 GitHub noreply 地址，避免公开本机主机名邮箱。
- 修改文件：`.gitignore`、`docs/README.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md`、`开发日志/2026-09-10.md`，以及提交中已纳入的当前产品代码、测试与文档。
- 验证结果：暂存差异和 `git diff --check` 通过；完整 iOS 576 项、Debug/Release 真机构建沿用本轮 12:09 已记录通过结果。GitHub CLI 的旧令牌已失效；常规 Git 推送在外传审批阶段被阻止，尚未把任何新提交发送到远程。
- 关键决定：目标仓库和完整上传范围需要用户再次明确确认；确认前不绕过审批、不改写远程历史。

### 12:29｜S16-11｜GitHub 产品快照上传完成

- 完成内容：收到用户对具体仓库、分支和 119 文件范围的明确确认后，将本地产品快照提交 `d71c1d3` 与发布准备记录 `dc8d49b` 推送到 `https://github.com/bethink0401/zhiheng.git` 的 `main` 分支。
- 修改文件：本次远程操作未改动产品代码；同步更新 `docs/README.md`、`docs/execution-steps.md`、`docs/implementation-plan-v1.md` 与本日志的发布状态。
- 验证结果：`git push origin main` 成功，远程从 `d00cf64` 前进到 `dc8d49b`；`git rev-parse HEAD`、`git rev-parse origin/main` 与 `git ls-remote origin refs/heads/main` 均返回 `dc8d49b22b8d4890f7f32d18a59b16400707179b`。上传内容不包含本地秘密、证书、构建产物、`output/` 或 `竞赛材料/artifacts/`。
- 关键决定：只进行普通快进推送，不强制覆盖远程历史；S16-11 完成后恢复 S14-12 真机交互确认。

### 19:28｜S12-19｜扩展普通 AI 对话引用事实

- 完成内容：普通 AI 事实包新增本地 7/28 天趋势、“今日变化”选中指标、今日和近 7 天三项感受、今日和近 7 天结构化生活事件，以及当前或最近微计划的模板、状态、日程、CareKit 完成/跳过/未记录、今日结果和最多 7 条反馈。AI 响应新增事实类别引用，客户端只接受本次事实包可用类别；提示词明确生活事件只能作为同期背景、反馈是主观记录，已有活动或暂停计划时不再推荐新计划。AI 助手发送时复用现有每日协调器、同期背景加载器和根级微计划会话，不新增持久化副本。
- 隐私与安全：事实包继续排除签到备注、事件备注、自定义事件名称、HealthKit 原始样本与来源标识、CareKit 计划/任务 ID 和其他底层记录 ID；代理新增顶层与补充事实字段白名单、评分/计数/强度/状态边界校验。应用内隐私数据流升级为 `s14-privacy-data-flow-v2`，与 `docs/privacy-data-flow.md` V1.1 同步。
- 修改文件：`Zhiheng/Zhiheng/AI/FactPack/HealthFactPackBuilder.swift`、`Zhiheng/Zhiheng/Domain/Models/HealthAIModels.swift`、`Zhiheng/Zhiheng/AI/Safety/HealthAISafetyRuleEngine.swift`、`Zhiheng/Zhiheng/AI/Networking/DeepSeekPersonalService.swift`、`Zhiheng/Zhiheng/Presentation/Assistant/AssistantView.swift`、`Zhiheng/Zhiheng/Presentation/Plans/MicroPlanView.swift`、`Zhiheng/Zhiheng/RootTabView.swift`、`Backend/AIProxy/server.py`、相关测试及项目/隐私文档。
- 验证结果：新增趋势/今日变化、结构化补充事实、隐私排除和事实引用测试通过；隐私数据流 10 项专项测试通过；最终完整 iOS 579 项通过、失败 0、跳过 0，结果位于 `Test-Zhiheng-2026.09.10_19-32-58-+0800.xcresult`；AI 开发代理 20 项通过；最终 Release 通用真机构建成功；`git diff --check` 通过。首次用 `CODE_SIGNING_ALLOWED=NO` 跑完整模拟器测试时，既有 Keychain 测试因测试包缺少签名权限返回 `-34018`，改用正常模拟器签名后该项和完整回归均通过。
- 关键决定：普通对话只发送结构化白名单事实，不复制任何备注、事件自定义名称或底层 ID；“未记录”“读取失败”和“演示模式”作为不同事实，不把缺失冒充为没有发生。用户提出的四类信息之外，同时纳入本地趋势证据、数据质量、来源变化/异常保护、时间范围和缺失状态，因为这些字段决定 AI 能否安全解释变化。

## 待办事项

- [x] 完成后台计算与缓存实现，运行首页展示和共享数据会话回归。
- [x] 删除洞悉页“本周最值得看”卡片，收敛洞悉、AI 助手、微计划和“我的”的主线程重复工作。
- [x] iPhone 16 Pro 重新连接后安装并打开最新 Debug 包。
- [ ] 用户在真实 HealthKit 数据下核对卡片进入/返回、四个标签切换、步数与活动能量。
- [x] 用户明确确认并已将 119 文件产品快照推送到 `bethink0401/zhiheng` 的 `main`。
- [x] S12-19 普通 AI 对话事实包扩展、引用校验、代理白名单和隐私说明同步完成。

## 问题与风险

- 真机包含远多于模拟器的数据量，同一份 90 天快照在 SwiftUI 多次刷新时同步重算仍会阻塞主线程；性能采样只包含调用栈与持续时间，不包含健康数值。
- 最新 Debug 包已安装并打开在 iPhone；代理无法代替用户触摸实体设备或确认真实健康数值，卡片返回与标签切换仍需用户现场确认后才能将 S14-12 标记完成。
- 当前工作区已有大量未提交改动；本任务仅收敛 S14-12 相关代码与文档，不覆盖其他工作。
- GitHub CLI 的旧令牌仍显示失效，但现有 Git HTTPS 凭据已成功完成推送；后续只有使用 `gh` CLI 管理 Issue、PR 或仓库设置时才需要重新登录。
- 普通 AI 对话现会在用户主动发送时带上更多结构化个人背景；虽然仍无自动请求并排除了备注与底层标识，但正式发布前仍需在 S14-02 日志审计和隐私政策中持续核对最小必要性与实际服务方条款。

## 下一步唯一任务

- S14-12：请用户在已打开最新包的 iPhone 16 Pro 核对卡片返回、洞悉、AI 助手、微计划和“我的”切换，以及步数和活动能量；验收后恢复 S14-02。
