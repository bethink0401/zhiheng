# 2026-09-14 开发日志

## 今日目标

- S14-13 / REQ-13：按用户插入要求，将演示模式完善为跨健康数据、主观感受、生活情境、微计划、计划评估、我的有效方法和 AI 事实包的连贯合成场景。

## 已完成事项

### 任务开始｜S14-13｜演示数据完整性与真实感

- 完成内容：已盘点现有演示分支。当前客观健康样本虽覆盖十六类映射，但 35 天数列使用简单取模和线性增长，数值过于机械；感受、生活事件、微计划和我的有效方法在演示模式被直接置空。
- 修改文件：`开发日志/2026-09-14.md`、`docs/implementation-plan-v1.md`。
- 关键决定：不向真实 SwiftData 或 CareKitStore 写入伪记录；演示内容必须使用独立、只读、可重现的合成事实源，并在页面上继续显著标记“演示数据”。所有趋势和计划结论仍由现有确定性引擎从合成事实计算，不为了好看而改阈值或写死宣传结论。

### 任务完成｜S14-13｜演示数据完整性与真实感

- 将主演示健康场景从 35 天机械变化扩展为 90 天十六指标：加入周内/周末节律、多个周期的自然波动、最近活动增加与睡眠缩短、一次设备缺失日，以及较早一次提前上床计划对应的时间窗口。睡眠阶段、逐日活动样本和设备来源口径继续走现有计算链，缺失没有补 0。
- 新增独立只读 `DemoSubjectiveRecordStore`，为同一虚构人物提供今日及近 7/28 天精力、压力、身体感受、可选备注，以及截止日、加班、咖啡因、较高强度运动和未佩戴设备等结构化事件；演示页可读但所有保存与删除都拒绝，真实 SwiftData 零读写。
- 演示微计划页现在展示已完成的“提前上床”5 天计划、4 次完成/1 次跳过、80% 进度、可选反馈、计划前冻结基线、生活背景、确定性结束评估、相关健康趋势和历史；演示刷新不访问 CareKitStore，创建、删除和隐藏真实内容均被会话边界拒绝。
- “我的有效方法”由两次独立演示计划运行生成，展示 2 次执行、80% 加权完成率、可判断数据质量、四档可信度、睡眠客观同期变化，以及精力/压力/身体感受的主观变化；不是写死的空卡或宣传结论。
- 洞察与普通 AI 对话通过显式注入的演示 Store 引用同一套合成感受、事件和计划事实；默认加载器仍禁止演示模式意外读取真实 Store。AI 对话有历史时也持续显示演示标识。
- 修复启动或设置切换数据模式时，微计划/“我的”可能沿用旧真实会话导致空白的问题：标签按模式重建；若旧模式的异步计划读取尚未结束，新模式等待收口后再加载。今日、洞察、AI、微计划和“我的”均保持显著“不是你的真实健康记录”标识。
- 修改文件：`DemoHealthDataService.swift`、`SubjectiveCheckInSession.swift`、`ContextEventSession.swift`、`TodayView.swift`、`InsightContextMatching.swift`、`InsightContextLoader.swift`、`SevenDayHealthSummary.swift`、`HealthFactPackBuilder.swift`、`AssistantView.swift`、`EffectiveMethod.swift`、`MicroPlanView.swift`、`RootTabView.swift`，以及对应测试和标准文档。
- 验证：`git diff --check` 通过；iPhone 17 Pro / iOS 26.5 模拟器完整 584 项 iOS 测试全部通过，无失败、无跳过；Release `generic/platform=iOS`、`CODE_SIGNING_ALLOWED=NO` 通用真机构建通过。
- 视觉验收：模拟器逐页确认今日页含演示横幅、73% 目标完成度、今日感受与变化；洞察页十六指标有自然折线和 7/28 天比较；AI 页能显示聚合事实且持续标明演示；微计划页含只读横幅、80% 进度、完成计划和结束评估；“我的”含筛选器和 1 张由两次运行汇总的方法卡。首次复验发现微计划模式切换竞态，修复后再次安装并确认计划正常显示。

## 待办事项

- [ ] S14-12：等待用户在已安装最新包的 iPhone 上确认真实 HealthKit 卡片返回与五标签切换。
- [ ] S14-02：S14-12 放行后检查日志、错误和分析代码不包含健康敏感数据。

## 问题与风险

- 当前工作区有 S12-20 及 S14-12 未提交改动；本任务只增量修改演示链路，不回退或覆盖现有改动。
- S14-13 无已知阻塞。模拟器验收不替代 S14-12 的真实 HealthKit 真机确认，本轮没有改写或放行该门禁。

## 下一步唯一任务

- 回到 S14-12，由用户在已安装最新包的 iPhone 上确认真实 HealthKit 卡片返回与标签切换；通过后继续 S14-02。
